Target: Claude Code CLI, рабочее дерево /home/me/code/content-factory-next, ветка main
Audience: оркестратор с правом читать и писать в этом дереве
Runtime: claude

Goal: довести до конца находки живого прогона продукта 01.09.2026 — тринадцать
задач в Beads, от неработающего с первого дня реестра источников до писем,
уходящих в спам. Не выдумывать четырнадцатую и не изображать завершение того,
что упирается во владельца.

Context: полное задание лежит в дереве —
`docs/prompts/content-section-orchestrator.md`. Прочитай его целиком до первого
действия, вместе с `AGENTS.md`, `CLAUDE.md`, `.codex/handoff.md` и
`docs/product/content-section-map.md`. Запусти `bd prime`.

Три вещи, которые нельзя узнать из задачи Beads и легко сломать:

1. В дереве лежат непроверенные глазом, но проверенные прогоном незакоммиченные
   правки — исправление P0 `content-factory-next-r14b` и часть `buts`. Убедись,
   что они на месте и зелены, и закоммить их отдельно **до** новой работы.
   Команды прогона — в задании, раздел «Состояние дерева».
2. Две задачи упираются не в тебя: `zudl` требует правки DNS владельцем, `4zef`
   ждёт макета писем. Для них работа — подготовить и остановиться, а не сдать
   половину как целое. Эпик раздела `odb8` наоборот строится полностью: решения
   приняты, макеты нарисованы заранее.
3. Корневой `.env` приведён к портам стенда и несёт перенесённые ключи. Не
   коммить его.

Entrypoint: Use $orchestrator-stage. Закрытие — Use $closeout (скиллы
`orchestration-bridge:orchestrator-stage` и `orchestration-bridge:closeout`).

Stop: остановись и спроси владельца, если работа упирается в одно из четырёх
непринятых решений (§«Решения, которые владелец НЕ принял» в задании), если
нужен доступ к DNS, публикация, деплой или платный вызов модели, или если
красный страж не краснеет при проверке. Не пушить и не открывать PR.

Output: волна за волной, как описано в задании. В конце — отчёт: что сделано и
чем проверено, что не начато и почему, какие вопросы ждут владельца. Каждая
закрытая задача Beads несёт в причине закрытия, что осталось непроверенным.
Если волна не сдвинулась — сказать прямо, а не сгладить.
