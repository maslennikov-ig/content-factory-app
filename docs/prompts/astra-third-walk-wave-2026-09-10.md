# Заказ оркестратору Codex (Астра): волна третьего захода 10.09.2026

Ты — оркестратор этой волны. Claude разобрал третий живой прогон владельца на боевом
`2fe4032ea3db`, сверил каждую находку с журналом и базой боевого, изучил два донора
(`/home/me/code/articles` — харнесс слопа и скиллы статей; `/home/me/code/helixa-2-lab`
— модуль ресерча), записал спецификацию, завёл эпик волны (29 задач, две закрыты как
слитые) и отдельный эпик ресерча (7 задач). Тебе — исполнение волны до выпуска, потом
эпик ресерча по фазам. Владелец вернётся к Claude после твоего слова «выпущено». Пиши
владельцу по-русски, коротко, с итогом первой строкой.

Слова владельца об итоге прогона: «Уже гораздо лучше, мы движемся прямо на правильном
пути». Это волна доводки стыков плюс две новые сущности: проверка с объяснениями
(три корзины правок, diff с пояснением при наведении) и «Перегенерировать» по инструкции.

## Что прочитать первым

1. `AGENTS.md`, `CLAUDE.md`, `.codex/handoff.md` (раздел «Next recommended»),
   `.codex/project-index.md`, `docs/design/component-inventory.md`.
2. **Спецификация волны**: `docs/product/third-walk-wave-2026-09-10-spec.md` —
   решения владельца дословно (§1), факты с боевого (§2), таблица «по умолчанию /
   опция» (§3), вопросы (§4), проверка/штампы/перегенерация (§5), ресерч (§6),
   приёмка (§7).
3. Эпик Beads `content-factory-next-tu3k.15` и задачи `.1`–`.29` (`bd show <id>`):
   у каждой — слова владельца, скриншот, файл:строка причины, исправление. `.24` и
   `.25` закрыты как слитые в эпик ресерча.
4. Эпик ресерча `content-factory-next-m0iy` (`.1`–`.7`); `.1` — уже принятые решения,
   читать первым; ждать нечего.
5. Материалы: `.codex/stages/content-factory-next-tu3k.15/evidence/walk-2026-09-10/`
   — `README.md`, `notes.txt`, `prod-facts.md`, `shots/`, шесть отчётов
   `explore-*.md` с полными цитатами кода (адаптация, календарь, аватар/карточка/
   каталог, «:null», донор articles, донор helixa). Причины подтверждены — не
   перепроверяй с нуля, читай отчёт и иди чинить.
6. Runbook выпуска: `docs/operations/production-deploy.md`, раздел «Обновление версии».

## Решения владельца (исполнять, не спрашивать)

- Аватар: поля и портрет принимаются автоматически, остаётся «Поправить» (`.15`).
- Карточка канала: везде, где уместно, «выбирает модель»; шкала эмодзи «без / мало
  1–3 / много / выбирает модель»; подсказки у каждого поля; тест, что каждое поле
  доезжает до промпта на обоих путях (`.4`, `.5`).
- Один индикатор хода на весь продукт из мастера аватара (`.14`).
- После адаптации акцентное «Опубликовать», рядом «Ещё ▾» (`.13`).
- Проверка: один текст с подсветкой, объяснение при наведении «как diff в коде»,
  три корзины правок, вопросы автору вместо удаления (`.10`); находки каталога
  штампов уходят в промпт, экран показывает «было N → стало M» (`.11`).
- «Перегенерировать»: одна кнопка, поле «Что перегенерировать?» свободным текстом,
  модель правит ровно это; заголовок — 3 варианта с гейтом честности (`.27`).
- Вопросы формулирует модель по материалу **до** написания сути, ≤3, один раунд;
  при адаптации — только если без человека никак (`.23`). Это решено, «да» не нужно.
- Каталог штампов дозаполнить из донора (`.28`), «Опоры текста» с типом (`.29`).
- Каталог компонентов достроить, стражи на копии геометрии (`.22`, `.21`);
  «ты сам подумай, как лучше с точки зрения UX» — по выделению карточки аватара (`.17`).

## Границы и правила (не обсуждаются)

- Ветка `wave/walk-2026-09-10` от `main`. Схема базы в волне не меняется: значения
  карточки — JSON `Integration.writingProfile`, темы аватара — `project.contentGoals`,
  заголовок — существующая колонка, контракт проверки — ответ двери, не таблица.
  `.26` — только комментарий в схеме. Если поток решит, что нужна колонка, —
  остановись и спроси владельца. `prisma db push` на боевой запрещён.
- Node 22.23.2 (`PATH=/home/me/.nvm/versions/node/v22.23.2/bin:$PATH`), pnpm 10.6.1.
  `tsc --noEmit` — ноль на трёх приложениях, держать так. Никаких `min-[…]`/`max-[…]`
  в Tailwind. Контракты `voice-wiring.contract.ts` и промпты под стражем ИИ — только
  версией. Сохранённые карточки каналов не должны «поехать»: старое `free` читается
  как `many` (тест).
- Субагенты: один поток — один worktree (`agent/<stream>`), `pnpm install
  --frozen-lockfile --prefer-offline` внутри; узкая проверка на поток, полный прогон
  один, в корне. Beads закрывать одной партией после завершения всех агентов, потом
  сверить `bd show` поимённо.
- Никаких секретов в чате, документах и коммитах. Никаких платных вызовов модели в
  тестах. Push только после `git fetch` и проверки, что удалённый не ушёл вперёд.
- **Выпуск** — по постоянному разрешению владельца от 07.09 («даю все разрешения, не
  останавливайся»), записать его в runbook как основание. До выпуска — стенд владельцу.

## Потоки и порядок

1. Сразу, параллельно:
   - **S1 календарь** (`.1`, `.2`) — `adaptation-picker.tsx`, `add.edit.modal.tsx`
     (переключатель → добавление), `piece.repository.ts`.
   - **S2 адаптация, промпт, вход** (`.3`, `.4`, `.5`, `.6`, `.7`, `.8`, `.9`, `.23`,
     `.29`) — `channel-directives.ts`, `channel-writing-profile.ts`, `writing-profile.*`,
     `intake.copy.ts`, `channel-questions.ts`, `core-questions.ts`, `intake.prompts.ts`,
     `intake.service.ts` (`textOrNull`, вопросы до сути, намерение vs содержание),
     `piece.service.ts` (roundOf, пересчёт заголовка), `agent.graph.service.ts`
     (генератор принимает `integrationId`), `post-length.ts`,
     `piece-channel-profile.tsx`, `PATCH /pieces/:id {title}`,
     `related-own-posts.note.tsx`. Стык: `.23` меняет порядок «суть → вопросы» на
     «вопросы → суть» — это самая рискованная правка волны, начинать с теста воронки.
   - **S4 индикатор** (`.14`) — `ui/progress.tsx` первым, остальные потоки берут его.
   - **S5 аватар** (`.15`, `.16`, `.17`, `.18`, `.19`) — `brand-voice/*`,
     `voice.service.ts`, `voice-directives.ts`, `ui/disclosure.tsx`.
   - **S6 таблица и каталог** (`.20`, `.21`, `.22`, `.28`) — `pieces.screen.tsx`,
     `content-search-words.tsx`, токен подсветки, `ui/filters-row.tsx`,
     `component-inventory.md`, страж «третья копия геометрии», `slop-rules.ru.ts`.
2. После S4 и S2: **S3 проверка и перегенерация** (`.10`, `.11`, `.12`, `.13`, `.27`)
   — контракт `changes[]` в `adaptation-review.ts`, `sentenceChanges` из
   `core-answer-diff.tsx`, `Hint` при наведении, `Disclosure`, контур находок как
   `REPAIR` в `core-write.ts`, общий пункт меню с подписью, двери `rewrite`.
3. Слияние по одному в корне: S4 → S6 → S1 → S2 → S5 → S3. Стыки: `piece.screen.tsx`
   (S2 панель / S3 блок / S4 Progress), `channel-directives.ts` (S2 эмодзи и `auto` /
   `.5` генератор), `ui/layers.tsx` (S3 пункт меню / S5 Toast), `intake.service.ts`
   (`.7` / `.23` / `.29`).
4. **Стенд для владельца** до выпуска: `cf-dev-*` поимённо, `localhost:4200`. Показать
   и попросить «да»: (а) вход с вопросами от модели до сути; (б) результат проверки
   одним текстом с объяснением при наведении; (в) «Перегенерировать» с инструкцией «только заголовок»;
   (г) аватар без «Принять» и экран «Аватар готов»; (д) карточка канала с «выбирает
   модель». Его правки — до выпуска.
5. Один полный прогон: `pnpm test` (три половины), `pnpm run build`, `git diff --check`,
   `scripts/orchestration/run_process_verification.sh`, квитанция
   `record-suite-receipt.sh` в основном потоке с длинным timeout.
6. Beads одной партией, причины закрытия — черновиком заранее.
7. Выпуск по runbook: commit + push в приватный, `prepare-public-tree.sh --update`,
   публичный коммит/push, `docker build` из публичного дерева, `verify-nginx-config.sh`,
   `push-image.sh`, `pull-image-on-host.sh`, `migrate diff` из образа (пуст), `mastra_*`
   до/после, `switch-host-image.sh`, `retain-host-artifacts.sh`. Откат `2fe4032ea3db`.
8. Записать выпуск: runbook, `.codex/handoff.md` (≤200 строк), `bd remember`,
   `docs/product/help-faq.md» («выбирает модель», «N штампов», «Проверить», «Переписать»).
9. Сказать владельцу: «выпущено <хэш>, откат <хэш>, что проверить» — и что четвёртый
   заход и замер живости (H1, мысль владельца, приготовленная Claude) готовит Claude.

## Эпик ресерча `m0iy` — сразу после волны, автономно

Владелец 10.09: «все разрешения даю… хочу, чтобы эпик выполнялся максимально
автономно». Все вопросы сняты заранее, ответы в `m0iy.1` и §6 спецификации: один
порт поиска, провайдер `'tavily' | 'openrouter' | 'exa'` настройкой организации,
поле ключа уже есть; Exa для ресерча, Tavily остаётся; адаптер Exa под форму
`WebResearchService`; SearXNG не ставим; уровень по умолчанию standard; квота
настройкой организации (quick 20 / standard 10 / deep 3 в месяц). Единственный
внешний вход — ключ Exa от владельца; без него живой прогон идёт на Tavily, адаптер
Exa проверяется записанными ответами. Порядок: `m0iy.2` → `.3` → `.4` → `.5`, стенд
владельцу, выпуск по runbook; `.6` и `.7` — после замера пользы первой фазы.
Ключи только в `.env` на хосте или в настройках организации; в репозитории — имя
переменной и runbook.

## Что считается сделанным

§7 спецификации. Плюс: `tsc` ноль, три половины `pnpm test` зелёные, стенд показан
владельцу, боевой переключён, `migrate diff` пуст, `mastra_*` без изменений, Beads
закрыты и сверены, runbook и handoff обновлены.
