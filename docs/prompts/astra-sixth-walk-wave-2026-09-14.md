# Заказ оркестратору Codex (Астра): волна шестого захода 14.09.2026

Ты — оркестратор этой волны. Claude разобрал шестой живой прогон владельца на боевом
`447e360f7007`, сверил каждую находку с журналом и базой боевого (три заготовки, журнал
операций, настройки суперадмина), записал спецификацию и завёл эпик из одиннадцати
задач. Тебе — исполнение волны до выпуска. Владелец вернётся к Claude после твоего
слова «выпущено». Пиши владельцу по-русски, коротко, с итогом первой строкой.

Волна одного правила. Владелец 14.09: «нужно максимально снимать с человека
когнитивную нагрузку»; «спрашивать только о тех вещах, которые… принесёт его личное
мнение… как интервью»; «подтвердить — модель сама должна подтверждать… проверка
фактов — добровольная кнопка»; «усилить ресерчем — не значит, что мы делаем проверки…
дописываем интересные факты, ссылки»; «опять мы по умолчанию что-то проверяем и
тратим поисковые запросы — зачем?». Каждая задача — вывод из этого правила.

## Что прочитать первым

1. `AGENTS.md`, `CLAUDE.md`, `PRODUCT.md` (правило «решаем за человека»),
   `.codex/handoff.md` (верхний блок), `.codex/project-index.md`,
   `docs/design/component-inventory.md`.
2. **Спецификация волны**: `docs/product/sixth-walk-wave-2026-09-14-spec.md` — слова
   владельца (§1), факты с боевого (§2), решения с файлами (§3), оценка заготовок (§4),
   границы (§5), приёмка (§6). Она уточняет §4 и §6 спецификации третьей волны.
3. Эпик Beads `content-factory-next-4zul`, задачи `.1`–`.11` (`bd show <id>`): в каждой
   — слова владельца, скриншот, файл:строка причины, исправление. `.11` — не твоя
   (страница седьмого захода, Claude). `75xn.35` зависит от `.4` и закрывается с ней.
4. Материалы: `.codex/stages/content-factory-next-4zul/evidence/walk-2026-09-14/` —
   `README.md`, `notes.txt`, `prod-facts.md`, `pieces/` (JSON трёх заготовок),
   `shots/`, три отчёта `explore-*.md` с файл:строка. Причины подтверждены — не
   перепроверяй с нуля, читай отчёт и иди чинить.
5. Runbook выпуска: `docs/operations/production-deploy.md`, «Обновление версии».
   Ловушка публичного коммита: `Source-Commit` и `Co-Authored-By` — один блок трейлеров
   без пустой строки между ними, иначе ворота квитанции не увидят коммит
   (`.codex/handoff.md`, блок 14.09).

## Решения владельца и Claude (исполнять, не спрашивать)

- Вопросы — только `thesis` и `position`, варианты от первого лица, поле `facts` из
  вопросов уходит (`.1`).
- Лан `pendingSearch` / `searchEnrichment` удаляется: без галочки — ни одного
  платного поиска (`.2`).
- «Опоры текста» — только строки с источником, пусто → раздел скрыт, `Hint` «?» у
  заголовка, подписи «Не подтвердилось и в текст не вошло» нет (`.3`).
- Под «Сутью»: «Перегенерировать» и «Дополнить ресерчем» — secondary одинаковые;
  «Проверить факты» — quiet только при `authorNumbers`; «Адаптировать» — единственная
  primary. Второй галочки на входе нет (`.4`, `.5`).
- «Дополнить ресерчем» = ресерч входа по сути → `ResearchOutcome` → `writeCore`; поля
  «чем обогатить» нет; режим `research` ревью удаляется (`.4`).
- «Проверить факты» = режим `web` ревью; `needs_context` не печатается; «нет
  подтверждения» → заметка, не вопрос; «Вопросы автору», «Сохранить ответы» и
  `reviewAnswer` удаляются (`.5`).
- Ответы — по смыслу: служебные фразы и адреса в текст не идут, ссылка в ответе
  читается `readLink` и становится источником, начало сути — целое предложение (`.6`).
- «Реши всё сама» только при ≥2 вопросах (`.7`); «Настройки канала» по центру (`.8`);
  `search.routing` только при своих ключах + вопрос в FAQ (`.9`); `scope local/global`
  в классификаторе запросов и честные подписи глубины (`.10`).

## Границы и правила (не обсуждаются)

- Ветка `wave/walk-2026-09-14` от `main`. Схема базы не меняется: всё в
  `ContentPiece.brief` (JSON) и в ответах дверей. Если поток решит, что нужна колонка,
  — остановись и спроси владельца. `prisma db push` на боевой запрещён.
- Node 22.23.2 (`PATH=/home/me/.nvm/versions/node/v22.23.2/bin:$PATH`), pnpm 10.6.1.
  `tsc --noEmit` — ноль на трёх приложениях, держать так (jest типы не проверяет).
  Никаких `min-[…]`/`max-[…]` в Tailwind. Контракты `voice-wiring.contract.ts`,
  `intake-v2.contract.ts` и промпты под стражем ИИ — только версией; удаление поля
  `facts` из вопросов — новая версия схемы вопросов, старые заготовки с `facts` в
  `questions.items` читаются и просто закрываются.
- Стражи дизайна: `Hint` — единственная подсказка; `cf`-токены; off-rhythm пиксели
  (`gap-[6px]`) страж не пропустит; `component-inventory.md` — реестр.
- Субагенты: один поток — один worktree (`agent/<stream>`), `pnpm install
  --frozen-lockfile --prefer-offline` внутри; узкая проверка на поток, полный прогон
  один, в корне. Beads закрывать одной партией после завершения всех агентов, потом
  сверить `bd show` поимённо (`bd prime` откатывает закрытия).
- Никаких секретов в чате, документах и коммитах. Никаких платных вызовов модели в
  тестах: ресерч и дайджест — записанными ответами. Push только после `git fetch`.
- **Выпуск** — по постоянному разрешению владельца от 07.09 («даю все разрешения, не
  останавливайся»). До выпуска — стенд владельцу.

## Потоки и порядок

1. Сразу, параллельно:
   - **S1 вход** (`.1`, `.2`, `.10`) — `intake.prompts.v2.ts`, `intake.service.ts`
     (`settleBrief`, `pendingSearch`, `finish`), `core-questions.ts`,
     `content-intake.dto.ts`, `web.research.service.ts` (классификатор, `scope`),
     `ai.clients.ts` (страна только при `local`), `intake.copy.ts`. Тесты
     `content-intake.flow`, `web.research`, записанные ответы.
   - **S2 страница заготовки** (`.3`, `.5`, `.6`, `.7`, `.8`) — `piece.screen.tsx`
     («Опоры текста», ячейка «Канал»), `piece-channel-profile.tsx`,
     `piece-questions.tsx`, `pieces.copy.ts`, `review-semantic.v2.ts`,
     `review-questions.tsx` (удалить), `piece.service.ts` (`answerReviewQuestions`,
     `briefWithAnswers`, поток `/answer` с `readLink`), `core-write.ts`,
     `adaptation-review.tsx` (кнопки). Тесты `piece.screen`, `core-write`,
     `piece answer`, `design.guard`.
   - **S3 настройки и справка** (`.9`) — `ai-provider.component.tsx`, `help-faq.md` +
     `help.copy.ts` одним коммитом.
2. После S1 и S2: **S4 «Дополнить ресерчем»** (`.4`) — `piece.service.ts` (новая
   дверь или режим на `/review`: `researchForIntake` + `digestResearch` по сути,
   снимок в Redis как на входе, «Продолжить» → `writeCore`), `adaptation-review.tsx`
   → `ResearchOutcome` из `intake.research.tsx`, удаление режима `research`. Самая
   рискованная правка волны — начинать с теста потока на записанных ответах.
3. Слияние по одному в корне: S3 → S1 → S2 → S4. Стыки: `piece.service.ts` (S2 / S4),
   `adaptation-review.tsx` (S2 кнопки / S4 экран), `intake.service.ts` (S1 / S4
   переиспользование).
4. **Стенд для владельца** до выпуска: `cf-dev-*` поимённо, `localhost:4200`. Показать
   и попросить «да»: (а) мысль с числами без галочки — заготовка сразу, без опор и
   без поиска; (б) с галочкой — «Проверили по источникам» и «Опоры текста» с «?»;
   (в) «Дополнить ресерчем» → находки → суть дополнена; (г) «Проверить факты» у текста
   с числами → diff; (д) чужой пост — два вопроса от первого лица, ответ со ссылкой.
5. Один полный прогон: `pnpm test` (три половины), `pnpm run build`, `git diff --check`,
   `scripts/orchestration/run_process_verification.sh`, квитанция
   `record-suite-receipt.sh` в основном потоке с длинным timeout.
6. Beads одной партией (`.1`–`.10` и `75xn.35`), причины закрытия — черновиком заранее.
7. Выпуск по runbook: commit + push в приватный, `prepare-public-tree.sh --update`,
   публичный коммит с одним блоком трейлеров, push, `make-source-archive.sh`,
   `docker build` из публичного дерева, `verify-nginx-config.sh`, проверка `/app/.env`
   в образе, `push-image.sh`, `CF_DEPLOY_HOST=helixa-prod pull-image-on-host.sh`,
   `migrate diff` из образа (пуст), `mastra_*` до/после, `switch-host-image.sh`,
   `retain-host-artifacts.sh`, три адреса 200. Откат `447e360f7007`.
8. Записать выпуск: evidence JSON в `.codex/stages/content-factory-next-4zul/`,
   `.codex/handoff.md` (≤200 строк, `Next stage id:` и `Recommended action:`),
   `bd remember`, `help-faq.md`.
9. Сказать владельцу: «выпущено <хэш>, откат <хэш>, что проверить» — и что страницу
   седьмого захода готовит Claude (`4zul.11`).

## Что считается сделанным

§6 спецификации. Плюс: `tsc` ноль, три половины `pnpm test` зелёные, стенд показан
владельцу, боевой переключён, `migrate diff` пуст, `mastra_*` без изменений, Beads
закрыты и сверены, handoff обновлён.
