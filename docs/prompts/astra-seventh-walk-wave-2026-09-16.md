# Заказ оркестратору Codex (Астра): волна седьмого захода 16.09.2026

Ты — оркестратор этой волны. Claude разобрал седьмой живой прогон владельца на боевом
`93aa33b85a79`, сверил каждую находку с базой боевого (четыре заготовки, журнал расхода
модели) и с кодом, записал спецификацию и завёл эпик из одиннадцати задач. Тебе —
исполнение волны до выпуска. Владелец вернётся к Claude после твоего слова «выпущено».
Пиши владельцу по-русски, коротко, с итогом первой строкой.

Прошлая волна (`4zul`) принята владельцем: «В целом всё очень даже неплохо». Эта волна —
три поломки, которые владелец увидел глазами, одна тихая (язык поиска), и пять его
просьб. Правило «решаем за человека» (`PRODUCT.md`) действует: всё новое —
необязательное, с умолчанием.

## Что прочитать первым

1. `AGENTS.md`, `CLAUDE.md`, `PRODUCT.md`, `.codex/handoff.md` (верхний блок),
   `.codex/project-index.md`, `docs/design/component-inventory.md`.
2. **Спецификация волны**: `docs/product/seventh-walk-wave-2026-09-16-spec.md` — слова
   владельца (§1), факты с боевого (§2), решения с файлами (§3), оценка заготовок (§4),
   границы (§5), приёмка (§6).
3. Эпик Beads `content-factory-next-xmfb`, задачи `.1`–`.11` (`bd show <id>`). `.11` — не
   твоя (страница восьмого захода, Claude).
4. Материалы: `.codex/stages/content-factory-next-xmfb/evidence/walk-2026-09-16/` —
   `README.md`, `notes.txt`, `prod-facts.md`, `explore-code.md` (файл:строка по каждой
   находке), `pieces/` (JSON cnt-11…14), `shots/`. Причины подтверждены — не
   перепроверяй с нуля.
5. Runbook выпуска: `docs/operations/production-deploy.md`, «Обновление версии».

## Решения владельца и Claude (исполнять, не спрашивать)

- Галочка «Берём» — по `factKey`; своя ошибка для отсутствующей строки (`.1`).
- «Проверить факты» в web-режиме — только фактические правки; валидация по правке, а не
  по отзыву; один повтор при непарсимом ответе; причина в журнал; упавшее ревью квоту не
  списывает (`.2`).
- Чужой пост определяет модель на шаге extract; при чужом посте без позиции человека —
  вопрос о позиции кодом. Ручного переключателя нет (`.3`).
- Классификатор поиска: чужой опыт как идея — `global`; классификация в журнал (`.4`).
- «Дополнить ресерчем» — окно `Dialog` с необязательным «Куда копать» и общим
  `ResearchLevelSelect`, умолчание «Стандартный» (`.5`, после `.6`).
- Подтверждение «Проверить факты» — `Dialog` (`.6`).
- Сортировка «Заготовок» — единый сортируемый `Th` + селект на карточках, `?sort=` в
  адресе, на клиенте (`.7`).
- Ключи поиска по движку: свой ключ движка перекрывает системный, «Вернуть на ключ
  системы», переключатель режима — только для генерации, поиск на своём ключе квоту не
  тратит; перед кодом `technical-premortem` (`.8`).
- Суть не спорит с позицией автора (`.9`); `ungrounded` и тезис пересчитываются после
  ресерча (`.10`).

## Границы и правила (не обсуждаются)

- Ветка `wave/walk-2026-09-16` **от `wave/walk-2026-09-14`** (выпущенный код, в `main`
  не слит). Схема базы не меняется; если поток решит, что нужна колонка, — остановись и
  спроси владельца. `prisma db push` на боевой запрещён.
- Node 22.23.2 (`PATH=/home/me/.nvm/versions/node/v22.23.2/bin:$PATH`), pnpm 10.6.1.
  `tsc --noEmit` — ноль на трёх приложениях (jest типы не проверяет). Никаких
  `min-[…]`/`max-[…]` в Tailwind. Контракты и промпты под стражем ИИ — только новой
  версией, старые заготовки читаются.
- Стражи дизайна: `Hint` — единственная подсказка, `Dialog` — единственное окно,
  `cf`-токены, `component-inventory.md` — реестр (сортируемый `Th` записать).
- Субагенты: один поток — один worktree (`agent/<stream>`), `pnpm install
  --frozen-lockfile --prefer-offline` внутри; узкая проверка на поток, полный прогон один,
  в корне. Beads закрывать одной партией после завершения всех агентов, потом `bd dolt
  push` и сверка `bd show` поимённо (`bd prime` откатывает закрытия).
- Никаких секретов в чате, документах и коммитах. Никаких платных вызовов модели в
  тестах — только записанные ответы. Push только после `git fetch`.
- **Диск хоста (`content-factory-next-hf97`, P1).** Перед `pull-image-on-host.sh`
  проверить свободное место; меньше 6 ГБ — остановиться и спросить владельца. Чистить
  только по `retain-host-artifacts.sh`, чужое не трогать.
- **Выпуск** — по постоянному разрешению владельца от 07.09 («даю все разрешения, не
  останавливайся»). До выпуска — стенд владельцу.

## Потоки и порядок

1. Сразу, параллельно:
   - **S1 вход и поиск** (`.3`, `.4`, `.9`) — `intake-kind.ts`, `intake.service.ts`
     (`openQuestions`), схема и промпт extract, `core-questions.ts`, `intake.prompts*.ts`,
     `core-write.ts`, `web.research.service.ts` (классификатор, журнал). Тесты
     `content-intake.flow`, `web.research`, `core-write` на записанных ответах.
   - **S2 страница заготовки** (`.1`, `.2`, `.6`, `.10`) — `piece.container.tsx`,
     `piece.screen.tsx`, `intake.research.tsx`, дверь `/facts` и DTO, `piece.service.ts`
     (`selectFact`, `acceptCoreResearch`), `review.v2.ts`, `review-semantic.v2.ts`,
     `ai.usage.service.ts` (квота за failed ревью), `adaptation-review.tsx` (`Dialog`).
   - **S3 «Заготовки»** (`.7`) — `ui/table.tsx` (`Th.sort`), `pieces.screen.tsx`,
     `pieces.container.tsx`, `pieces.adapter.ts`, `component-inventory.md`.
   - **S4 ключи поиска** (`.8`) — премортем, `ai.provider.config.ts`, `ai.search-tasks.ts`,
     учёт расхода, `ai-provider.component.tsx`, `ai-provider.copy.ts`, страж
     `ai.search-routing`, `help-faq.md` + `help.copy.ts` одним коммитом.
2. После S2: **S5 «Дополнить ресерчем»** (`.5`) — `ResearchLevelSelect` из
   `intake.screen.tsx`, `PieceResearchDto`, `piece-research.contract.ts`,
   `researchExistingCore`, окно на `adaptation-review.tsx`.
3. Слияние по одному в корне: S3 → S4 → S1 → S2 → S5. Стыки: `adaptation-review.tsx`
   (S2 / S5), `intake.service.ts` (S1 / S2 / S5), `web.research.service.ts` (S1 / S4).
4. **Стенд для владельца** до выпуска: `cf-dev-*` поимённо, `localhost:4200`. Показать и
   попросить «да»: (а) «Берём» снимается и ставится; (б) «Проверить факты» — окно и
   отзыв без 502; (в) чужой пост WB/Ozon — вопрос о позиции; (г) «Дополнить ресерчем» —
   окно с «Куда копать» и глубиной; (д) сортировка «Заготовок»; (е) ключ одного движка и
   «Вернуть на ключ системы».
5. Один полный прогон: `pnpm test` (три половины), `pnpm run build`, `git diff --check`,
   `scripts/orchestration/run_process_verification.sh`, квитанция
   `record-suite-receipt.sh` в основном потоке с длинным timeout.
6. Beads одной партией (`.1`–`.10`), причины закрытия — черновиком заранее.
7. Выпуск по runbook: commit + push в приватный, `prepare-public-tree.sh --update`,
   публичный коммит с одним блоком трейлеров (без пустой строки между `Source-Commit` и
   `Co-Authored-By`), push, `make-source-archive.sh`, `docker build` из публичного дерева,
   `verify-nginx-config.sh`, `push-image.sh`, проверка диска, `CF_DEPLOY_HOST=helixa-prod
   pull-image-on-host.sh`, `migrate diff` из образа (пуст), `mastra_*` до/после,
   `switch-host-image.sh`, `retain-host-artifacts.sh`, три адреса 200. Откат `93aa33b85a79`.
8. Записать выпуск: evidence JSON в `.codex/stages/content-factory-next-xmfb/`,
   `.codex/handoff.md` (≤200 строк, `Next stage id:` и `Recommended action:`),
   `bd remember`.
9. Сказать владельцу: «выпущено <хэш>, откат <хэш>, что проверить» — и что страницу
   восьмого захода готовит Claude (`xmfb.11`).

## Что считается сделанным

§6 спецификации. Плюс: `tsc` ноль, три половины `pnpm test` зелёные, стенд показан
владельцу, боевой переключён, `migrate diff` пуст, `mastra_*` без изменений, Beads
закрыты и сверены, handoff обновлён.
