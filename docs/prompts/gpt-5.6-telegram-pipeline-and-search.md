Target: GPT-5.6 в coding-agent среде с shell, Git и Beads, рабочий каталог `/home/me/code/content-factory-next`.
Audience: ручная передача от владельца репозитория. Ты — ведущий инженер этого продукта.

Namespace note (2026-08-14): prompt examples use the current
`@contentfactory/*` import contract; the pipeline stage did not own that
migration.

Goal: Довести открытую часть Beads-эпика `content-factory-next-ja3` — Telegram content pipeline MVP, включая задачи веб-поиска `ja3.23`–`ja3.29`, — оставив репозиторий проверенным и незакоммиченным.

Success criteria:
- Каждая взятая задача закрыта в Beads вместе с доказательством, которое её закрыло. Задача без доказательства не закрывается.
- Список открытых задач эпика сокращается только выполненной работой, а не сужением формулировок.
- На финальном дереве проходят: `pnpm run build`, `pnpm test`, `node scripts/branding/brand-scan.cjs`, `pnpm run docs:check`, `scripts/orchestration/run_process_verification.sh`, `git diff --check`.
- Given пост генерируется на русском по международной теме, When отрабатывает шаг исследования, Then веб-запрос уходит на английском и источники не ограничены русскоязычными.
- Given пост по теме, специфичной для российского рынка, When отрабатывает шаг исследования, Then добавляется второй запрос на языке предмета и в справку попадают оба набора источников.
- Given две организации с разными настройками поиска, When обе генерируют одновременно, Then каждая использует свою, а изменение настройки вступает в силу без рестарта процесса.

Context:
- Истина по задачам — Beads, а не этот промпт. Выполни `bd ready` и `bd show <id>`: у каждой задачи своё описание и свои критерии приёмки, включая пути к файлам и уже поставленные диагнозы дефектов. Не выводи их заново.
- Прочитай сначала: `AGENTS.md`, `CLAUDE.md`, `.codex/handoff.md`, `.codex/project-index.md`, `docs/README.md`.
- Порядок: сначала разблокировать (`ja3.2` CORS, `ja3.4` язык контента, `ja3.3` длина поста), затем поиск (`ja3.23` → `ja3.24` → `ja3.25`, далее `ja3.26`, `ja3.27`, `ja3.29`), затем Stage 3 (`ja3.5`) и зависящее от него (`ja3.28`, `ja3.6`).
- Поиск — это обогащение информации для написания контента, а не локализация. Язык запроса следует за тем, где лежит информация, а не за тем, для кого пишется пост: английский по умолчанию, локальный запрос добавляется, только когда локален сам предмет.
- Движок поиска — настройка организации, а не выбор в коде. Реализуй его как конфигурацию; значение по умолчанию выбирается отдельным пилотом и решается не тобой.
- Английский и русский равноправны, главного нет. Новые пользовательские строки добавляются в `en` и `ru` одной правкой.
- Graphify — локальный код-граф только на чтение: читай `graphify-out/GRAPH_REPORT.md` и делай точечные `graphify query` до широкого поиска по файлам.
- В `docs/research/` лежат два отчёта по выбору поискового бэкенда. Их неязыковые выводы полезны; их языковой вывод отброшен владельцем и не является основанием ни для чего.

Constraints:
- Beads-задача до кода, статус in progress при начале работы.
- Не коммить, не пушь, не создавай remote, PR и деплой. Работа остаётся незакоммиченной для ревью.
- Не переименовывай импорты `@contentfactory/*`, legacy env-ключи, идентификаторы Prisma и таблиц, имена Temporal, слаги провайдеров, параметры OAuth и контракты публичного API.
- Не делай платных вызовов к провайдерам, реальных OAuth-подключений и живых публикаций, не спросив: назови конкретный вызов и его стоимость.
- Не добавляй второго поискового вендора, тяжёлую зависимость, Playwright или Cypress, не спросив.
- Никаких новых `TODO/FIXME/HACK/XXX`. Реальный перенос оформляется задачей в Beads и строкой в `.codex/handoff.md`.
- Репозиторий публично безопасен: ни секретов, ни учётных данных, ни живых идентификаторов аккаунтов в Git.

Output:
Сначала поведение: что пользователь теперь может сделать, чего не мог, и как это увидеть. Затем по каждой задаче — идентификатор Beads, доказательство приёмки и ограничения. Затем точный результат каждой команды проверки. Затем что перенесено и почему. Прямо назови всё, что проверить не удалось.

Stop:
Остановись и задай один конкретный вопрос перед: платным вызовом, внешней записью, коммитом или пушем, добавлением вендора или зависимости, изменением совместимого идентификатора, а также если критерии приёмки задачи невыполнимы как написаны. Не сужай задачу молча.
