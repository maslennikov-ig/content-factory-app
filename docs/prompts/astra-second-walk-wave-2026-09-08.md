# Заказ оркестратору Codex (Астра): волна второго захода 08.09.2026

Ты — оркестратор этой волны. Claude подготовил разбор, задачи, спецификацию, макеты и
handoff; тебе — исполнение до выпуска на боевой. Владелец вернётся к Claude после твоего
слова «выпущено». Пиши владельцу по-русски, коротко, с итогом первой строкой.

## Что прочитать первым

1. `AGENTS.md`, `CLAUDE.md`, `.codex/handoff.md` (раздел «Wave «второй заход 08.09»»),
   `.codex/project-index.md`.
2. Спецификация: `docs/product/second-walk-wave-2026-09-08-spec.md` — решения владельца
   дословно, причины, потоки, поведение приёмки.
3. Эпик Beads `content-factory-next-tu3k.14` и его 25 задач (`bd show <id>`): у каждой —
   слова владельца, скриншот, файл:строка причины, исправление, поток.
4. Заметки и скриншоты: `.codex/stages/content-factory-next-fn33/evidence/walk-2026-09-08/`.
5. Макеты: `docs/design/desert-lab/pipeline/README.md` и холст по ссылке в нём.
6. Runbook выпуска: `docs/operations/production-deploy.md`, раздел «Обновление версии».

## Границы и правила (не обсуждаются)

- Ветка `wave/walk-2026-09-08` от `main`. Схема базы не меняется; если поток решит, что
  нужна колонка, — остановись и спроси владельца. `prisma db push` на боевой запрещён.
- Node 22.23.2 (`PATH=/home/me/.nvm/versions/node/v22.23.2/bin:$PATH`), pnpm 10.6.1.
  `tsc --noEmit` — ноль на трёх приложениях, держать так. Никаких `min-[…]`/`max-[…]`
  в Tailwind. Не мутировать контракты `voice-wiring.contract.ts` — только версией.
- Субагенты: один поток — один worktree (`agent/<stream>`), `pnpm install
  --frozen-lockfile --prefer-offline` внутри; узкая проверка на поток, полный прогон один,
  в корне. Beads закрывать одной партией после завершения всех агентов, потом сверить
  `bd show` поимённо (закрытия откатываются, пока агенты живы).
- Никаких секретов в чате, документах и коммитах. Никаких платных вызовов модели в
  тестах. Push только после `git fetch` и проверки, что удалённый не ушёл вперёд.
- **Выпуск** — по постоянному разрешению владельца от 07.09 («даю все разрешения, не
  останавливайся»), записать его в runbook как основание. До выпуска — стенд для владельца.

## Порядок

1. Сразу: S1 (публикация и промпт), S2 (аватар), S5 (копи), S6 (навигация A), S8 (замер по базе).
2. Навигация выбрана владельцем 08.09: **вариант A** (`NavConveyor.dc.html`). S6 стартует
   сразу вместе с потоками из п. 1; спрашивать не нужно.
3. После макетов (они уже одобрены как основа; владелец смотрит результат на стенде):
   S3 (вход и вопросы), S4 (страница заготовки v2 и таблица), S7 («Проверить ▾»).
4. Слияние по одному в корне: S5 → S1 → S2 → S8 → S3 → S4 → S7 → S6. Стыки:
   `piece.service.ts` (S1 hintsOf / S3 answer / S7 review), `piece.screen.tsx` (S3 слот
   вопросов / S4 раскладка / S7 кнопка), `intake.service.ts` (S1 строки промпта / S3
   снятие короткого пути).
5. **Стенд для владельца**: поднять `cf-dev-*` (`docker compose` поимённо, см. handoff:
   `localhost:4200`, не `127.0.0.1`), написать владельцу: «страница заготовки и таблица
   на стенде, посмотрите cnt-01». Его правки — до выпуска.
6. Один полный прогон: `pnpm test` (три половины, читать все три), `pnpm run build`,
   `git diff --check`, `scripts/orchestration/run_process_verification.sh`, квитанция
   `record-suite-receipt.sh` в основном потоке с длинным timeout.
7. Beads одной партией, причины закрытия — черновиком заранее.
8. Выпуск по runbook: commit + push в приватный, `prepare-public-tree.sh --update`,
   публичный коммит/push, `docker build` из публичного дерева, `verify-nginx-config.sh`,
   `push-image.sh`, `pull-image-on-host.sh`, `migrate diff` из образа (должен быть пуст),
   `mastra_*` до/после, `switch-host-image.sh`, `retain-host-artifacts.sh`. Откат
   `5f657ccf294e`. В выпуск входит 8fa803b2 (ряд фильтров таблицы, ещё не развёрнут).
   `main.ts` меняется (сжатие) — до выпуска проверить на стенде, что потоки входа и
   разбора отдают первую строку сразу.
9. Записать выпуск: runbook (раздел выпусков), `.codex/handoff.md` (≤200 строк),
   `bd remember` запись выпуска, `docs/product/help-faq.md` («Что уже написали» больше нет,
   «Где смотреть расход», «Проверить ▾»).
10. Сказать владельцу: «выпущено <хэш>, откат <хэш>, что проверить на боевом» — и что
    третий заход по странице живого теста готовит Claude.

## Что считается сделанным

Раздел 4 спецификации. Плюс: `tsc` ноль, три половины `pnpm test` зелёные, стенд
показан владельцу, боевой переключён, `migrate diff` пуст, `mastra_*` без изменений,
Beads закрыты и сверены, runbook и handoff обновлены.
