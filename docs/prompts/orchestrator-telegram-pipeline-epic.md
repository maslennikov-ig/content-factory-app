Target: external orchestrator (ChatGPT app), capable coding model with repository write access
Audience: manual launcher handing the epic to another system

Namespace note (2026-08-14): compatibility guidance below uses the current
`@contentfactory/*` import contract; namespace migration is not part of this
historical epic handoff.

Goal: implement the Beads epic `content-factory-next-ja3` in
`/home/me/code/content-factory-next` so that a Telegram channel is filled
regularly on two languages and the pipeline's own throughput is visible.

Success criteria:
- Every open child of `content-factory-next-ja3` is either closed with evidence
  or explicitly deferred with a reason recorded in Beads. Nothing is silently
  dropped.
- Given a visitor whose `i18next` cookie is `ru`, when any server-rendered page
  loads, then the server HTML and the client render agree and React reports no
  hydration mismatch; and given two concurrent requests in different languages,
  each gets its own language.
- Given a Russian channel, when a post is generated by the editor assistant,
  the chat agent, the generator or autopost rewriting, then the text comes back
  in Russian, and the same request with English selected comes back in English.
- Given a Telegram post opened through the statistics button, then the screen
  states that this channel reports no per-post platform figures and shows what
  can be enabled instead, rather than an unexplained empty state.
- A production analytics view reports published volume, failure rate with
  reasons, lead time from creation to publication and origin mix
  (`creationMethod`), computed from the local database with no platform call.
- Focused checks for each changed surface pass, `node scripts/branding/brand-scan.cjs`
  reports zero unexplained references, and
  `scripts/orchestration/run_process_verification.sh` passes.

Context:
- `AGENTS.md` is the authoritative repository contract; `CLAUDE.md` is its
  compact entrypoint. Read both before changing code.
- `docs/product/telegram-pipeline-mvp.md` carries the plan, the wave order and
  the constraints already established by API probes and code reading. Treat its
  "Границы, установленные при разборе" section as settled; do not re-litigate
  it. In particular: per-post views and forwards are unreachable through the
  Bot API and MTProto is excluded by the owner.
- `.codex/handoff.md` holds current state, `.codex/project-index.md` navigation.
- Runtime is Node `22.23.2` from `.nvmrc` and pnpm `10.6.1` with the committed
  lockfile. Never npm or yarn.
- A local stack is already running against a throwaway Postgres/Redis pair and
  a real test channel; `.env` is Git-ignored and holds working credentials.

Constraints:
- Write zone: this repository only. `/home/me/code/content-factory` is a
  read-only donor.
- Beads is the only durable task tracker. Claim before starting, close with
  evidence, and add new issues rather than widening an existing one.
- Do not mutate an upstream-used Temporal workflow or activity contract; add a
  versioned successor and migrate callers.
- Keep provider-specific behavior inside provider implementations.
- Preserve AGPL and upstream attribution and compatibility-sensitive
  identifiers: `@contentfactory` package names, environment variable names, database
  and provider identifiers.
- New interface work uses the `cf-*` semantic tokens and the shared primitives
  in `apps/frontend/src/components/ui` and
  `libraries/react-shared-libraries/src/form`.
- Resolve version-sensitive dependency behavior through documentation rather
  than guessing; if no external boundary applies, record why.
- Ask the owner for missing product intent whenever two or more outcomes stay
  plausible and the answer changes behavior, acceptance, scope or rework — for
  example where the content language should be stored, or whether the
  statistics button should be hidden or explained. A workable technical default
  does not remove that need.
- Ask for authority separately and only at the exact external or hard-to-reverse
  action: connecting an account, publishing to a real channel, a paid model
  call, a database migration against existing data, a push, or opening a pull
  request. Do not ask for permission when the missing thing is a product
  decision, and do not seek confirmation for ordinary local edits.

Output:
- Lead with what changed in user-visible terms and how to verify it by hand.
- State which normal, failure and edge scenarios were exercised, and name the
  ones that were not.
- List every limitation and every deferral, with the Beads id.
- Supporting evidence after the narrative: commands run with their results, and
  the changed files.
- Record any decision-affecting finding a later agent would otherwise lose:
  evidence, implication, confidence, and where it belongs.

Stop:
- Stop and report when a wave's success criteria are met, before starting the
  next wave.
- Stop and ask when a change would require altering a Temporal contract, when a
  fix needs a schema migration touching existing rows, or when a task turns out
  to depend on a product decision the owner has not made.
- Stop and report rather than working around a failing brand scan or process
  verification.
