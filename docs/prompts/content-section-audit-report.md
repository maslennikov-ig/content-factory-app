# Отчёт аудита волны 01–02.09.2026

Адресат: оркестратор, который вёл `docs/prompts/content-section-orchestrator.md`.
Дерево: `/home/me/code/content-factory-next`, `main`, вершина `11fe62a3` плюс
незакоммиченные правки аудита. Дата: 02.09.2026. Задача аудита: `content-factory-next-vro5`.

## Вердикт

Работа сделана добротно: контракт Temporal соблюдён буквально и защищён стражем,
изоляция арендаторов сплошная, четыре SQL совпадают со схемой, переводы полны на
шестнадцати локалях, причины закрытия честные. Но «все задачи выполнены» неточно:
из семнадцати закрыты тринадцать, две закрытые не выдержали проверки, одно решение
владельца реализовано против него, и всё это ушло на боевую.

## Что я исправил (в дереве, не закоммичено)

| Находка | Где | Что теперь |
|---|---|---|
| `tyrk` P1: единый контекст всегда пуст — производители не создавали оценку, принять её негде | `source-registry.repository.ts`, `content-fact.{service,repository}.ts`, `content-context.{builder,finalize}.ts`, `content-context.controller.ts`, `content-facts.*` | Правило §9.5 в коде: своё слово `VERIFIED` при создании, свой материал `ACCEPTED`, поиск `PROPOSED`; жест `confirmEvidence` и `POST /facts/:factId/evidence/:evidenceId/confirm`; кнопка «Подтвердить» только на строках «найдено поиском»; `copyFact` по тому же правилу; своё слово чтит истёкший `freshUntil` |
| §9.4 нарушен: «Материалы» и «Что уже написали» — две вкладки, закреплено тестом | `content-section.screen.tsx`, `content-section.copy.ts`, `content-section.route.test.cjs` | Пять вкладок, архив — переключатель вида внутри «Материалов», `?tab=archive` ведёт туда же |
| «Вернуть» воскрешал факт, заменённый копией | `content-fact.repository.ts`, витрина | Только `RETRACTED`; `SUPERSEDED` → 409 `CONTENT_CONTEXT_FACT_SUPERSEDED` |
| P1: без политики повторов одно недоступное письмо останавливало всю очередь `send_email_v2` | `send.email.workflow.v2.ts` | `maximumAttempts: 5`, `scheduleToCloseTimeout: 30m`; страж `email-workflow-v2-retry-bound.guard` |
| Вечный процесс проверки подписок без `continueAsNew` | `content-lead-check.workflow.ts` | Сброс истории каждые 100 проходов |
| Упавший периодический запуск нечем восстановить; `setState` мёртв | `content-lead.service.ts`, контроллер | Ручная «Проверить сейчас» досоздаёт процесс с `USE_EXISTING`; PAUSED пропускается, ERRORED проверяется и возвращается в ACTIVE; `setState` удалён |
| Позиционный `externalId` навсегда глотал новые поводы | `lead-feed.gateway.ts` | sha256 от `url|title|publishedAt` вместо индекса |
| Привязка Telegram не перепроверяла `isSuperAdmin` при `/start` | `telegram.updates.service.ts` | Добавлено в `where` |
| `tsc --noEmit` бэкенда красный с 30.08 (два места brand-voice) | `brand-voice.types.ts`, `text-spots.ts`, `voice.service.ts` | Ноль ошибок на трёх приложениях |
| Runbook предписывал схему после переключения образа, а handoff называл это ломающим вход | `production-deploy.md` | Нумерованные шаги переписаны: diff и валидатор в одноразовом контейнере нового образа, `up -d` — десятый шаг |
| Абзац runbook утверждал, что почта не умеет сообщать о сбое | там же | Переписан как история |
| Четыре `*-schema-apply.sql` не упомянуты, три Telegram-колонки не названы | там же | Дописано |
| Перенос меток в этап знал три языка из шестнадцати | `editorial-stage-schema-apply.sql`, `editorial-stage.tag-migration.test.cjs` | Все шестнадцать, сгенерированы программой; тест на полноту |
| Страж переноса меток всегда SKIP: переменная не выставлялась нигде | `run-docker-backed-ci.sh`, `docker-ci-contract.test.cjs` | Третий постгресный набор в docker-CI, страж контракта доведён до шести команд |
| `LEAD_FEED_CHECK_ENABLED` и `EMAIL_PROVIDER` нигде не задокументированы | `.env.example`, `app.env.example`, `outbound-connections.md` | Добавлены |
| `interpolate` подставлял параметры в HTML без экранирования | `backend-strings.ts` | Экранирование, тест |
| Заказы дизайна требовали отменённые две вкладки и несли опровергнутый диагноз SPF | `content-section-design-brief.md`, `email-template-design-brief.md` | Приведены к §9.3, §9.4 и `email-deliverability-spf.md` |
| Мелочь | подсказка брифа про ручную вставку id, мёртвый ключ `notFactsBodyAlt`, комментарии со ссылкой на несуществующий §5, устаревший комментарий архива про вид поиска | Исправлено |

Каждый новый страж показан красным до правки. Приёмка на дереве: jest 254 набора,
3307 прошли, 1 пропущен; `node --test` 113 / 0 / 4 пропущено; python 29 OK;
`tsc` 0 на `frontend`, `backend`, `orchestrator`; `git diff --check` чисто;
process verification OK. Квитанция и оба отчёта рецензентов:
`.codex/stages/content-factory-next-vme/evidence/audit-2026-09-02/`.

## Beads

- `lh5s` **переоткрыта**: производитель `acceptSearchResult` есть, но
  `POST /sources/search-evidence` не вызывает ни один файл фронтенда, экрана
  поиска нет вовсе. «Найдено поиском» недостижимо из продукта.
- `tyrk` открыта, комментарий с описанием правки; закрывать после коммита и
  живого прохода.
- Новые: `fkft` (P1, решение владельца), `ni7x`, `cl19`, `w4vh`, `th1s` (P3).
- `vro5` — запись аудита, закрыта.

## Что осталось

1. **Коммит** правок аудита — за владельцем. Около 54 файлов, все проверены.
2. **Живой проход** на стенде: бриф → факт → копилот. Ни один экран в этой волне
   не открывался в браузере; всё доказано только тестами и типами.
3. **`fkft`.** Выпуск 02.09, применение схемы на боевой, остановка `send_email` v1
   и `push` в `origin/main` были исключены разделом «Полномочия» без отдельного
   слова владельца по каждому пункту; записи о таком слове нет ни в handoff, ни в
   runbook, ни в Beads. Это вопрос владельцу, не тебе. Если разрешение было, его
   надо записать в `production-deploy.md` по образцу прошлых выпусков.
4. **`lh5s`:** экран, где человек видит результат поиска и жмёт «принять» —
   по карте это бриф. Дверь подтверждения и кнопка на витрине уже есть.
5. **Открытое, без изменений:** `rrs9` (обучение описывает только контур
   Postiz), `4zef` (макет письма нарисован, письмо не переоформлено), `odb8.4`
   (поиск по словам не начат), четыре P3 выше.
6. **Вопрос владельцу, не решать за него:** ворота брифа пропускают утверждение
   с любой ссылкой, единый контекст — нет. Планка контекста теперь достижима, но
   бриф на голых ссылках из поиска по-прежнему даст черновик без единой цитаты.

## Правила, которые эта волна подтвердила

- Зелёный набор доказывает узел, не проводку: `lh5s` закрыта по тесту
  производителя, до которого нет пути; страж переноса меток был вечным SKIP.
- `tsc --noEmit` — отдельная проверка, теперь ноль на трёх приложениях; держать так.
- Handoff ограничен 200 строками стражем `test_orchestration_closeout.py`.
- Добавил постгресный набор в `run-docker-backed-ci.sh` — обнови
  `docker-ci-contract.test.cjs`, он считает команды поштучно.
- Границы действий: выпуск, DNS, `push`, секреты — по отдельному слову владельца,
  записанному там, где его найдёт следующий читатель.

---

## Второй аудит, 03.09.2026: проверка отчёта «всё сделано» по `93092c84..04c7c2f3`

**Вердикт.** Работа сделана, и сделана честно: чек-лист первого аудита закрыт
делом (экран поиска, живой проход, обучение, письма, `fkft` записан в runbook),
приёмка на исходном дереве зелёная, `tsc` по нулям. P1 нет. Два ревьюера нашли
восемь P2, шесть починены в дереве, два вынесены владельцу. Задача `w4ij`.

### Починено в дереве (не закоммичено)

| Находка | Файлы | Что теперь |
|---|---|---|
| Участник и редактор могли удалить, выключить и включить канал — удаление вместе со всеми постами; матрица обещала обратное | `integrations.controller.ts`, `roles-matrix.md`, `launches/menu/menu.tsx`, `tests/roles-matrix.guard.test.cjs` | три двери под `ADMIN`, три строки в матрице, пункты меню у участника скрыты; страж показан красным до правки |
| Отклонённое агентство не получало письма (`p3gq`): после `approved: false` выборка искала `approved: true` | `agencies.repository.ts`, `agencies.service.ts`, `tests/agency-decline-email.guard.test.cjs` | отдельный запрос для решения без фильтра по одобрению; тест на обе ветки, красный до правки |
| Участник мог нажать «переподключить канал» → 403 → переход на `undefined` | `launches.component.tsx`, `platform-analytics/render.analytics.tsx`, 16 × `translation.json` | клик и кнопка только администратору; участник видит «канал отключён, попросите администратора» |
| Решение о двух планках записано тремя способами: код «решено владельцем», handoff «открыто», карта молчит | `voice-brief.adapter.ts`, `content-section-map.md` §9, handoff | одно: вопрос открыт, бид `z0b0` |
| Runbook: абзацы удержания 01.09 читались как часть выпуска 03.09; выпуск `a63227c58446` отсутствовал в хронике | `production-deploy.md` | абзацы возвращены под свой выпуск; запись `a63227c58446` с перечнем того, чего выпускавшая сессия не записала (digest, вес, адреса, удержание) |
| Починка падения `/content?tab=brief` без стража | `tests/content-section-tabs.boundary.guard.test.cjs` | модуль вкладок без `'use client'`, страница читает из него; `resolveContentTab` на всех значениях; красный на исходном дефекте |
| Handoff противоречил себе про `fkft`; карта говорила «живого прохода не было» | handoff, карта | приведено к правде, 200 строк |

### Записано в Beads

- Причины закрытия дописаны комментариями в `tyrk`, `lh5s`, `4zef`, `fkft` — закрыты они были без причины.
- `saas.2`: предложено, что считать «сквозным доказательством», иначе задача вечная.
- Новые: `jjvz` P2 (комментарии к посту через границу арендатора), `d1rx` P2 (решение владельца: утверждение из фрагмента поиска — «ваше слово» до «Подтвердить»?), `z0b0` P2 (решение владельца: две планки), `nq7e`, `za05`, `5w6u` P3.

### Доделано в тот же день по слову владельца («даю все разрешения, сделай всё сразу»)

- `jjvz`: комментарий к посту проверяет, что пост принадлежит организации и не удалён; публичный список комментариев пуст для удалённого поста. Страж `tests/post-comments-tenant.guard.test.cjs` красный до правки.
- `z0b0` и `d1rx` решены по поручению из правила §9.5 и записаны как допущения в карте раздела §10: планки остаются разными; фрагмент из поиска больше не предзаполняет утверждение, он цитируется рядом с формой. Страж экрана поиска проверяет пустое поле.
- Коммит и push в `origin/main`, удержание на хосте — см. handoff.

### Что осталось

1. `saas.2` закрывать после одного набора на живой базе с двумя организациями.
2. Выкладка сделана в тот же день: `a4f1863f9010` (правки аудита), затем `efafe77fe64e` (две починки тестов, из-за которых CI публичного репозитория был красным три выпуска подряд). Обе записи в runbook; CI на работающем коммите зелёный.
3. `nq7e`, `za05`, `5w6u` — P3.

### Проверено и в порядке (не перепроверять)

Роль читается в одном месте, `EDITOR` не проходит `ADMIN`-двери, impersonate требует `isSuperAdmin` из базы, DTO не дают назначить `SUPERADMIN`; SQL совпадает со схемой, порядок частей А/Б исключает половинчатое применение; починки `editTag` и `getMediaById` доведены до всех вызовов; письма экранируются один раз; онбординг и поиск за middleware с организацией из запроса; allowlists дизайна только убавились; стражи поиска и письма рендерят реальный DOM.
