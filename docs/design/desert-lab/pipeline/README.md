# Макеты волны второго захода 08.09.2026

Референс, не исходник продукта: артборды Claude Design для эпика
`content-factory-next-tu3k.14`, показаны владельцу 08.09.2026 до правки кода. Холст:
`https://claude.ai/code/artifact/c4a5109a-6956-47c7-acfe-6a78f3721a0a`.
Заметки владельца — `.codex/stages/content-factory-next-fn33/evidence/walk-2026-09-08/notes.md`,
спецификация — `docs/product/second-walk-wave-2026-09-08-spec.md`.

Что сверялось с продуктом: боковое меню 248 px, строка 32 px с радиусом 8 и отступом 10
(`new-layout/sidebar.tsx`), шапка 56 px (`layout.component.tsx:143`), токены `cf-*` из
`../content/tokens.css`, кнопки 40/32, чипы 24, панели с радиусом 8 (те же классы, что в
`../pieces/`). Каждый артборд несёт чип `dark` (переключатель темы).

| Файл | Экран | Что решено |
| --- | --- | --- |
| `NavConveyor.dc.html` | вариант A навигации | Меню в порядке пути с номерами шагов: С чего начать → Аватар → Каналы → Заготовки → Календарь → Аналитика; «Контент» распадается на пункты. «Каналы» — список каналов, не календарь. |
| `NavStepper.dc.html` | вариант B навигации | Меню короткое; наверху раздела «Контент» шаги «Мысль → Заготовка → Адаптации → Публикация», аватар и каналы — настройки пути справа. |
| `NavHub.dc.html` | вариант C навигации | Заготовка — центр: меню из трёх пунктов, аватар и каналы живут в панелях страницы заготовки. |
| `PiecePageV2.dc.html` | `/content/pieces/[id]` | Отступы как у раздела; «В архив» в тулбаре; суть → «Уточнить суть» под ней → «Куда адаптировать» с карточкой канала в панели → адаптация как один раскрываемый объект с «Проверить ▾»; справа «Что модель поняла» без сырых значений и «Опоры текста»; изменённые после ответа предложения подсвечены. |
| `PiecesTableV2.dc.html` | `/content?tab=materials` | Клик по строке открывает заготовку, стрелка раскрывает на месте; «ещё нет» без платного вызова; фильтр «архив»; полосы «Что уже написали» нет; «2 поста» в клетке; подсветка по основе слова. |
| `IntakeOneField.dc.html` | `/content?tab=brief` | Одно поле «О чём будем писать», блока «Куда» нет; один живой индикатор хода. |
| `QuestionsOptions.dc.html` | вопросы адаптации | Варианты — кнопки; призыв не спрашивается, если задан в карточке канала; «В какой форме рассказать?». |
| `CheckMenu.dc.html` | объект адаптации | «Проверить ▾» с тремя режимами и ценой у каждого; результат разницей; «Принять» / «Оставить как было». |

**Владелец выбрал 08.09 вариант A («Конвейер в меню», `NavConveyor.dc.html`); B и C остаются
как отклонённые.** Правка этих
файлов запрещена: следующая волна делает свой холст.
