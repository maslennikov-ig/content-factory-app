# Холст «Экран адаптации» — десятый заход, 22.09.2026

Референс, не исходник продукта: артборды Claude Design, показанные владельцу 22.09.2026.
Холст: `https://claude.ai/code/artifact/c2c00ddb-b415-4c83-9748-71b87d31a2ba`.
Beads: `content-factory-next-97dq.37` (экран адаптации), `.38` (слои настроек), `.39` (сквозная проверка), `.41` (присланный текст).

Владелец выбрал **вариант A** («мне однозначно нравится вариант А»): одна заготовка, первая вкладка «Суть»,
дальше по вкладке на канал со значком-клеткой состояния; во вкладке канала всё до публикации; окно
«Создать пост» из заготовки не открывается. B и C лежат на втором листе холста как отложенные.

| Файл | Что |
| --- | --- |
| `Main.dc.html` | A · вкладка Telegram: варианты, ручная правка, «Убрать следы ИИ» с заменой, «Для этого поста», предпросмотр, дата и «Запланировать» |
| `CoreTab.dc.html` | A · вкладка «Суть»: «Что вы прислали» свёрнуто, суть, действия, «Куда дальше», «Что мы поняли», «Опоры» |
| `NewChannel.dc.html` | A · новый канал: один вопрос перед текстом (интервью адаптации) |
| `OptionB.dc.html`, `OptionC.dc.html` | отложенные варианты |
| `SettingsLayers.dc.html` | аватар — кто говорит, канал — куда, «Для этого поста» — разово |
| `EditorFate.dc.html` | что переезжает из окна «Создать пост», что уходит, что остаётся |
| `Audit.dc.html` | итоги сквозной проверки (полный отчёт: `.codex/stages/content-factory-next-97dq/evidence/walk-2026-09-22-tenth/coherence-audit.md`) |

Каждый артборд несёт чип `dark`. Правка запрещена: следующая волна делает свой холст.
