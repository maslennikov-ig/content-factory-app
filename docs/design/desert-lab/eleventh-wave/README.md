# Холст «Экраны двенадцатой волны» — одиннадцатый заход, 23.09.2026

Референс, не исходник продукта. Холст: `https://claude.ai/code/artifact/01dfe256-b723-4e87-853a-503b17b9ee59`.
Beads: `content-factory-next-97dq.48` (Для этого поста + превью), `.49` (нижняя полоса), `.50` (календарь, «Что публикуем»), `.51` (профиль, способы входа, глобальные).
Исходники артбордов собираются из `src/*.body.html` скриптом `python3 src/build.py`.

| Лист | Артборды |
| --- | --- |
| Вкладка канала | `Main` (A: настройки поста всегда открыты, превью по переключателю), `PostB` (B: настройки в окне, превью справа) |
| Календарь | `CalDayA` (день списком слотов), `CalDayB` (день сеткой часов), `CalWeek` (неделя и месяц, одна карточка), `Picker` («Что публикуем») |
| Настройки | `ProfileA`, `ProfileB`, `SignIn`, `Global` |

Выбор владельца 23.09: «Я склоняюсь к варианту А все-таки» — берём A везде (вкладка канала A, день A, профиль A); неделя/месяц, «Что публикуем», способы входа и глобальные — как на холсте.
