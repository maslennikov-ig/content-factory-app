---
colors:
  light:
    canvas: "#EFE9DB"
    surface: "#FBF8F0"
    surface-subtle: "#E4DDCB"
    surface-raised: "#FFFDF6"
    ink: "#22231A"
    ink-muted: "#5C5D4C"
    ink-inverse: "#F4F1E4"
    border: "#D3CAB3"
    border-strong: "#B0A68A"
    border-control: "#847B62"
    navigation: "#E4DDCB"
    navigation-text: "#22231A"
    navigation-muted: "#5C5D4C"
    navigation-active: "#FBF8F0"
    accent: "#3F6B2E"
    accent-hover: "#33581F"
    accent-soft: "#E1EAD2"
    accent-ink: "#FFFFFF"
    signature: "#9C6A16"
    info: "#2F5A82"
    info-soft: "#E3EBF3"
    warning: "#7A4D00"
    warning-soft: "#F6E7C2"
    danger: "#8E2B22"
    danger-soft: "#F6E0DB"
    focus: "#3F6B2E"
    backdrop: "rgba(34, 35, 26, 0.46)"
    overlay-shadow: "0 16px 32px -12px rgba(34, 35, 26, 0.26)"
  dark:
    canvas: "#14150F"
    surface: "#1C1E16"
    surface-subtle: "#22241B"
    surface-raised: "#272A1F"
    ink: "#ECEBDF"
    ink-muted: "#A6A794"
    ink-inverse: "#14150F"
    border: "#33362A"
    border-strong: "#4B4F3E"
    border-control: "#7E826D"
    navigation: "#0E0F0A"
    navigation-text: "#ECEBDF"
    navigation-muted: "#A6A794"
    navigation-active: "#22241B"
    accent: "#7FB03A"
    accent-hover: "#96C551"
    accent-soft: "#1F2915"
    accent-ink: "#0F1409"
    signature: "#C8922A"
    info: "#7FA6C8"
    info-soft: "#161F28"
    warning: "#D9A02B"
    warning-soft: "#2A2010"
    danger: "#D2705C"
    danger-soft: "#2C1512"
    focus: "#7FB03A"
    backdrop: "rgba(8, 9, 5, 0.74)"
    overlay-shadow: "0 18px 44px -14px rgba(0, 0, 0, 0.72)"

typography:
  families:
    sans:
      name: "Geologica"
      license: "SIL OFL 1.1"
      scripts: "latin, cyrillic"
      axes: "wght"
      source: "локальный файл в репозитории"
    mono:
      name: "JetBrains Mono"
      license: "SIL OFL 1.1"
      scripts: "latin, cyrillic"
      axes: "wght"
      source: "локальный файл в репозитории"
      features: "tnum, zero"
  tokens:
    heading-xl: { family: sans, size: 32px, weight: 650, line-height: 1.2, tracking: "-0.02em" }
    heading-lg: { family: sans, size: 24px, weight: 650, line-height: 1.25, tracking: "-0.015em" }
    heading-md: { family: sans, size: 18px, weight: 650, line-height: 1.35, tracking: "0" }
    body-lg:    { family: sans, size: 16px, weight: 400, line-height: 1.55, tracking: "0" }
    body-md:    { family: sans, size: 14px, weight: 400, line-height: 1.5, tracking: "0" }
    body-sm:    { family: sans, size: 13px, weight: 400, line-height: 1.45, tracking: "0" }
    label-md:   { family: sans, size: 13px, weight: 650, line-height: 1.3, tracking: "0" }
    label-sm:   { family: mono, size: 12px, weight: 600, line-height: 1.3, tracking: "0.04em" }
    caption:    { family: mono, size: 11px, weight: 500, line-height: 1.35, tracking: "0.02em" }
    display-num: { family: mono, size: 32px, weight: 600, line-height: 1.1, tracking: "0", features: "tnum" }

spacing: [4, 8, 12, 16, 20, 24, 32, 40, 48]
page-gutter: 24
sidebar: { expanded: 248, collapsed: 72 }

rounded:
  none: 0
  sm: 4
  md: 8
  lg: 12
  full: 9999

components:
  button-primary:   { background: accent, text: accent-ink, radius: 8, height: 40, padding: 12 }
  button-secondary: { background: surface, text: ink, border: border-control, radius: 8, height: 40, padding: 12 }
  input:            { background: surface, text: ink, border: border-control, radius: 8, height: 40, padding: 12 }
  panel:            { background: surface, text: ink, border: border, radius: 8, padding: 20 }
  status:           { radius: full, padding: 8 }
---

# Content Factory — визуальный язык

## Обзор

Система остаётся тем же инструментом с теми же ролями токенов, сеткой и плотностью. Меняется характер: вместо тёплой бумажной редакции — цех и лаборатория. Тёмная тема становится основной и проектируется как самостоятельная композиция; светлая остаётся полноценной дневной, выгоревшей на солнце, а не осветлённой копией тёмной.

Три материала задают тон: необработанный бетон — нейтрали, реагент — зелёный акцент, окисленный металл — охра подписи. Поверхность читается как плоскость с фактурой цвета: уровни различаются тоном и границей, а не объёмом. Свет жёсткий и боковой — это высокий контраст плоскостей и чёткая граница, а не свечение.

Экран занят почти целиком нейтралями. Цвет появляется там, где есть действие, выбор, фокус или состояние, о котором нужно знать. Рабочая информация — метка, номер, дата, статус — набирается моноширинным, как трафаретная маркировка на металле.

## Цвет

**Нейтрали.** Тёмная тема построена от `canvas #14150F` — почти чёрный с оливковым уклоном, не синий и не серый. Три плоскости над ним (`surface`, `surface-subtle`, `surface-raised`) отличаются на 6–13% светлоты: разница видна, но не расслаивает экран. Светлая тема начинается с песка `#EFE9DB`; `surface` светлее канвы, `surface-subtle` темнее, что даёт вложенным зонам глубину без тени.

**Зелёный — только работа.** `accent` обозначает главное действие, текущий выбор, фокус и положительное управляемое состояние. Он не заливает крупные плоскости и не используется как фон настроения. В тёмной теме это едкий реагентный `#7FB03A` — 7.15:1 к канве; в светлой плотный `#3F6B2E` — 5.17:1. Поле `accent-soft` намеренно тихое: в тёмной теме почти чёрная зелень, в светлой пыльная.

**Охра — подпись.** `signature` появляется в знаке, в маркировке партии и в редких точках узнавания: не больше одного-двух пятен на экран. Кнопочной системой не становится, рядом с главным действием не ставится.

**Статусы.** `info` стальной, `warning` латунь, `danger` запёкшийся кармин. В тёмной теме кармин осветлён до `#D2705C`, иначе текст статуса не проходит 4.5:1; насыщенный `#8E2B22` остаётся в светлой теме. Каждый статус несёт три носителя смысла: цвет, текст и пиктограмму; ни один не различается только цветом.

**Фокус.** Кольцо 2px цветом `focus` с внешним отступом 2px. Отступ заполняется цветом подложки, поэтому кольцо остаётся видимым и на акцентной кнопке: между зелёным кольцом и зелёной заливкой всегда лежит полоса фона. Глобальное отключение обводки запрещено.

## Типографика

Основная гарнитура — **Geologica**: SIL OFL 1.1, кириллица и латиница в одном файле, переменная ось `wght`. Она остаётся — техническая, с узкими апертурами, ровная на русском.

Добавляется вторая — **JetBrains Mono**: SIL OFL 1.1, полноценная кириллица, переменная ось `wght`, табличные цифры и зачёркнутый ноль. Ей принадлежат три токена: **`label-sm`**, **`caption`** и **`display-num`** — крупное измеренное число (объём набранных знаков, средняя длина фразы). `display-num` ставится по одному на зону и всегда рядом с подписью, что именно измерено; заголовком он не работает. Остальные семь токенов — Geologica.

Моноширинный отвечает за рабочую информацию: идентификаторы публикаций и партий, даты и время, числовые колонки таблиц, метки каналов, трафаретные маркеры. Верхний регистр и трекинг `0.04em` разрешены только для `label-sm` внутри рамки маркера — как способ отличить служебную метку от подписи, а не как общий декоративный приём. В остальных местах регистр обычный.

Заголовки — `text-wrap: balance`, длинные пояснения — `text-wrap: pretty` и мера 65–75 знаков. Русский и английский набираются одной парой гарнитур и не меняют компоновку.

## Макет

Ритм 4px, рабочий шаг 8px. Отступы: плотные элементы 8–12px, панели 16–24px, между смысловыми зонами 24–32px. Поле страницы 24px.

Навигация 248px развёрнутая, 72px свёрнутая. Ниже 1024px сворачивается, ниже 768px становится выезжающей панелью. Контрольные ширины 1440, 1024, 768, 390.

В светлой теме навигация перестала быть тёмной планкой: рельс набран песком `surface-subtle` и отделён `border-strong`. Причина техническая — зелёный акцент светлой темы на тёмном рельсе даёт 2.69:1 и не проходит порог 3:1 для маркера выбора. Причина композиционная — дневная тема перестаёт быть светлой оболочкой вокруг тёмного каркаса. В тёмной теме рельс, наоборот, ушёл ниже канвы до `#0E0F0A`: свет падает на рабочую область, а не на навигацию.

## Глубина

Иерархия строится плоскостью, границей и пространством. Обычная панель не сочетает широкую тень с рамкой — только рамка. Тень `overlay-shadow` разрешена исключительно слоям над контентом: меню, popover, dialog, toast. Цветных теней и свечения нет.

Шкала слоёв неизменна: base → sticky → dropdown → backdrop → modal → toast → tooltip. `backdrop` в тёмной теме — почти непрозрачная копоть 74%, в светлой — плотная тень песка 46%.

Движение 150–250ms, ease-out, только на смену состояния. При `prefers-reduced-motion` движение отключается или заменяется мгновенным переходом.

## Формы

Базовый радиус 8px: кнопка, поле, панель, карточка. 12px — крупные оболочки: диалог, выезжающая панель, оболочка календаря. 4px — трафаретные маркеры и мелкие метки. Полный pill — только статусы, фильтры и компактные переключатели. Радиус 24px и больше не применяется.

Карточка элемента — прямоугольник с рамкой `border`, заливкой `surface`, верхней строкой маркировки (моноширинный код и дата) и корпусом. Вложенные карточки запрещены: внутри используются секции, разделители и `surface-subtle`.

## Компоненты

Один тип кнопки, поля, статуса и пункта навигации на весь продукт. Высота управляемых элементов 40px, плотный вариант 32px — таблицы и панели фильтров.

- **Кнопки.** `primary` — заливка `accent`, текст `accent-ink`. `secondary` — `surface` и рамка `border-control`. `quiet` — без фона и рамки, по наведению фон `surface-subtle`. `destructive` — заливка `danger`, текст `ink-inverse` в обеих темах (7.5:1 в светлой, 5.4:1 в тёмной). Загрузка: кнопка сохраняет ширину, подпись заменяется индикатором и остаётся доступной для скринридера.
- **Поля.** Подпись сверху `label-md`, helper снизу `body-sm` цветом `ink-muted`, ошибка — рамка `danger` и текст с пиктограммой. Placeholder цветом `ink-muted` проходит 4.5:1.
- **Навигация.** Активный пункт: фон `navigation-active`, левая планка 3px `accent`, вес 650. Свёрнутый рельс 72px оставляет пиктограмму и всплывающую подпись.
- **Статусы.** Pill с рамкой, пиктограммой и текстом. Заливка — `*-soft`, текст и рамка — цвет роли.
- **Таблица.** Плотная: строка 36px, разделители `border`, числа и даты моноширинным с табличными цифрами, числа выровнены вправо.
- **Слои.** Dialog — `surface-raised`, радиус 12, `overlay-shadow`, backdrop. Popover и меню — `surface-raised`, радиус 8. Toast — `surface-raised` с полосой роли слева 3px.

Нажатое состояние (`active`) кнопок задаётся внутренней тенью `inset 0 2px 0 rgba(0, 0, 0, .14)` для светлых поверхностей и `.22` для заливок — это единственное значение цвета вне палитры токенов, оно описывает нажатие, а не роль.

Обязательные состояния каждой поверхности: loading, empty, default, selected, success, восстановимая ошибка, ограничение доступа, disabled, длинный контент.

## Иконографика

Один линейный набор, толщина 1.5–1.8px, оптическая рамка 20–24px, скруглённые концы, без заливок. Пиктограмма всегда сопровождается текстом там, где несёт смысл. Emoji вместо интерфейсных пиктограмм не используются. Аватары каналов и метки провайдеров — двухбуквенный символ в рамке, тот же приём, что и в компактном знаке.

## Делать и не делать

**Делать:** держать почти всю площадь на нейтралях; ставить зелёный только на действие, выбор, фокус и положительное состояние; отделять уровни тоном и границей; давать каждому объекту видимый идентификатор, дату и статус; писать коротко и утвердительно; проверять контраст расчётом.

**Не делать:** градиентный текст, стекломорфизм, декоративные сетки и паттерны на рабочих поверхностях, радиус 24px и больше, широкую тень вместе с рамкой, emoji вместо иконок, стену одинаковых карточек, отдельную палитру для раздела агентов, свечение и цветные тени, анимацию ради впечатления, охру как вторую кнопочную систему, трафаретную метку на каждом элементе.
