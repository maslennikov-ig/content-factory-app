# Stage content-factory-next-fn33

Status: accepted; the authoritative result is `acceptance-receipt.json`.

Восемь задач живого прогона выполняются строго в порядке
`fn33.1 → fn33.3 → fn33.2 → jdfy → 3r4a → f4ai → yyiy → fn33.4` на ветке
`work/walkthrough-2026-09-03`. Каждый поток оставляет отдельный артефакт и
один коммит задачи; итоговая приёмка принадлежит root.

Запрещены push, merge, deploy, доступ к серверу, запись в production database,
платные вызовы и работа с реальными пользователями.

Все восемь продуктовых задач реализованы отдельными коммитами. Восьмая
вначале была отложена после трёх одинаковых Luna API `404`, затем тот же поток возобновлён
после восстановления сервиса и вернул GREEN. Независимый security-review нашёл и повторно
проверил защиту мутаций, атомарность и обратно совместимое хеширование длинных
многобайтовых паролей.

docs-reviewed: updated - матрица ролей и реестр компонентов приведены к новому поведению; остальные изменения полностью описаны в восьми stream artifacts.
project-index: reviewed-no-change - стабильные точки входа проекта и команды не изменились.
graph-reviewed: used - устаревший граф `94fdcb33` применялся только как ориентация, а решения подтверждались по текущим файлам; изменения точечные, поэтому refresh для этой границы не требуется.
documentation-decision: no external/versioned boundary - lockfiles и внешние API-контракты не менялись.
independent-review: complete - повторная reviewer-сессия после восстановления API подтвердила CSRF-границы, Serializable/retry и атомарное членство; финальная проверка password hashing записана в review-потоке.

## Explicit defers

Нет. Браузерные прогоны, для которых нужны тестовые аккаунты, остаются ручной проверкой перед поставкой.
