# Plan

1. Verify licences, pricing, maintenance, network behaviour, feature coverage,
   build cost, and integration fit from current primary sources.
2. Map the existing media picker, upload endpoint, modal shell, store, tests,
   and browser-review seam.
3. Select one primary foundation and one fallback; record what evidence would
   reverse the decision.
4. Write executable RED contracts for the eight-feature floor, no-vendor
   network boundary, Cyrillic export, upload round-trip, states and keyboard.
5. Implement the smallest production editor and stable local review scenes.
6. Run focused checks, real-browser network and responsive proof, independent
   review, then one root-owned stage acceptance.
