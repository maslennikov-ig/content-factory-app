Target: /home/me/code/.worktrees/cf-measure, ветка `codex/pl1-measure`
Audience: рабочая сессия-исполнитель
Runtime: Claude Code CLI

Goal: две вещи по порядку. Сначала **ревизия**: четыре задачи эпика открыты, а по коду видно, что работа сделана — `pl1.2` (голос доходит до генератора), `pl1.4` (три корпуса), `pl1.6` (норма), `pl1.8` (направления). Сверь приёмку с кодом и с числами прогона `*-2026-08-27-t16`, закрой выполненное, остальное сведи к названному решению владельца. Потом **`pl1.5`** — перекалибровать мерку похожести против настоящего противника: текста, который продукт сам и пишет. Условия и приёмка в задачах, читай через `bd show`, не пересказывай себе заново.

Context: прочитай `.codex/handoff.md`, `.codex/stages/content-factory-next-pl1/evidence/README.md` целиком и коммит `93f7737a` целиком — в его сообщении вся арифметика 27.08.2026, и она здесь не повторяется. Код: `voice-calibration.ts`, `voiceprint.ts`, `lineup.ts`, `character-ngrams.ts`, стенд `scripts/evidence/voice-eval/`.

Это трек A. Параллельно идёт трек B (`pl1.33`, отбор черновиков) в дереве `/home/me/code/.worktrees/cf-draft-pick`. Ты в его файлы не пишешь, он в твои — тоже. Твоя зона: `content-intelligence/brand-voice/` и стенд. Его зона: `agent/` и `variants.cjs`.

Почему `pl1.5` важнее прочего. Порог сегодня снимается на текстах чужих людей. На боевой 28.08.2026 это дало у всех трёх голосов нижнюю границу **ноль**: «не похоже» выносится только тексту с ровно нулём голосов, и в ту же дыру попадает от 5% до 10% собственных постов автора — 3 из 38, 4 из 51, 2 из 29. Порог честен, слепа мерка: при 3% пропущенных чужих людей она пропускает 2%, 23% и 45% генераций.

**Первый шаг бесплатен.** Генерации вчерашнего прогона сохранены — `evidence/runs/{owner,avetov,britva}-2026-08-27-t16/generations.json`, по 2,1–2,4 МБ. Пересчёт порога против них не стоит ни одного вызова. Новый прогон нужен только для проверки результата.

Порядок `pl1.5`: снять порог против сохранённых генераций вместо чужих людей и записать, где встали границы, даже если ответ неприятен; проверить составную мерку с привычками поста и оставить её, только если она поднимает парный тест на всех трёх корпусах; решить судьбу полосы «не могу сказать» — отдельно взвесь названный владельцем 28.08 вариант не выносить «не похоже» при нулевой нижней границе вовсе; переписать §3.8 спецификации, где стоит 89,7%, снятые против лёгкого противника.

Граница задачи: не «отличать человека от машины вообще», а «отличать ЭТОГО человека от машины, пишущей на его темы».

Success criteria: четыре задачи ревизии либо закрыты по своей приёмке, либо переформулированы с объяснением, почему. `pl1.5` доведена до порога, снятого против генерации, с записанными долями ошибок по трём корпусам. Полный Jest зелёный. `tsc --noEmit` не даёт новых ошибок сверх двух унаследованных (`text-spots.ts:315`, `voice.service.ts:1113`). Каждая новая красная проверка проверена подменой того, что она стережёт.

Constraints:
- Платные вызовы — только с явного разрешения владельца, каждый раз заново. До любого платного шага проверить остаток на счёте OpenRouter, ключ в чат не выводить. `--tier flex` обязателен: без него прогон стоит вдвое.
- Боевую не трогать. `prisma db push` не применять никогда: на стенде он снесёт 27 таблиц `mastra_*`.
- Кэш корпуса и `generations.json` остаются вне репозитория — в них чужие тексты, `.gitignore:99` это держит, не ослабляй.
- Ни одно чужое предложение не должно попасть в базу, промпт, экран, журнал или свидетельство. В отчётах только числа. `tests/brand-voice.routes.test.cjs` обязан остаться зелёным.
- Beads ведёшь только ты: две сессии, пишущие одновременно, теряют закрытия. Закрывай одной партией, сразу `bd dolt push`, сверь поимённо через `bd show` — вывод самой `bd close` рапортует успех и до отката.
- Меняешь арифметику мерки — двигай `NGRAM_MEASURE_VERSION` или `CALIBRATION_VERSION` и объясняй, что именно изменилось.

Ловушки: `export PATH="$HOME/.nvm/versions/node/v22.23.2/bin:$PATH"` обязателен, иначе берётся Node 24 и `public-funnel-runtime-proof` падает на проверке версии — это не регрессия. Полный Jest оставляет фронтенд отвечающим 500, пока не удалить `apps/frontend/.next`. Правки в `libraries/` требуют удалить `apps/backend/dist` и `tsconfig.tsbuildinfo`. `tsc --noEmit` — отдельный шаг, Jest типы не проверяет.

Output: правки с проверками, коммиты в `codex/pl1-measure`, запись в `evidence/README.md` о том, что измерено и что осталось неизвестным, обновлённый `.codex/handoff.md` (потолок 200 строк, старое ужимать, а не дописывать сверху). Ветку не сливать: слияние в `codex/content-bugs` делает корневой сеанс, когда оба трека закончат.

Stop: остановись и спроси, если нужен платный прогон; если пересчёт против генераций показал, что мерка непригодна и нужен другой признак — это решение о направлении, а не о коде; если для приёмки нужен взгляд владельца («узнаю ли я себя» в `pl1.6`).
