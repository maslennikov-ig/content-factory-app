Target: /home/me/code/content-factory-next, ветка codex/content-bugs, worktree /home/me/code/.worktrees/cf-cj4
Audience: рабочая сессия-исполнитель
Runtime: Claude Code CLI

Goal: подготовить стенд замера к прогону на трёх корпусах. Задачи content-factory-next-pl1.27 и pl1.28 — условия, границы и приёмка в них самих, прочитай через bd show и не пересказывай себе заново. Платных вызовов в этой работе нет, кроме разбора трёх корпусов на локальном стенде.

Context: прочитай .codex/handoff.md и .codex/stages/content-factory-next-pl1/evidence/README.md. Коротко: 26.08.2026 корпус и разбор в продукте стали принадлежать аватару, а не пространству (pl1.26), и на боевой живут три аватара с раздельными корпусами. Стенд этого ещё не умеет: corpora.cjs читает по organizationId и в таком пространстве соберёт три корпуса в один — тот самый дефект, который из продукта убрали. Файлы выгрузок лежат локально: tmp/font-preview/result.json (Масленников), tmp/font-preview/result copy.json (Аветов), tmp/anton.json (Бритва). Скрипт расстановки и его README — .codex/stages/content-factory-next-pl1/evidence/avatars/.

Success criteria: measure на каждом из трёх корпусов даёт разное число образцов, совпадающее с продуктом — 153/1023/125, обучающая часть 108/717/88. На локальном стенде три аватара с разобранными и активированными голосами, у каждого свой корпус и свой портрет.

Constraints: боевую не трогать, работа целиком на локальном стенде. Не тянуть кэш боевой базы: он несёт расшифрованный ключ AI. Локальная схема отстаёт от рабочей — догнать её предпросмотром migrate diff и точечным psql; prisma db push допустим только на пустой локальной базе без таблиц Mastra. Кэш корпуса остаётся вне репозитория: в нём чужие тексты.

Ловушки: /home/me/.local/bin/node затеняет nvm, ставь PATH явно. Jest не проверяет типы — гони npx tsc -p apps/backend/tsconfig.build.json --noEmit. Правки в libraries/ не подхватываются, пока живы apps/backend/dist и tsconfig.tsbuildinfo. Полный Jest оставляет фронтенд отвечающим 500, пока не удалить apps/frontend/.next. Prisma отвергает null внутри in — про nullable-колонку спрашивают через OR. Beads откатывает закрытия при работающих агентах: закрывай одной партией, потом bd dolt push, потом сверяй через bd show.

Output: правки в scripts/evidence/voice-eval/ с тестами, коммиты в codex/content-bugs, закрытые pl1.27 и pl1.28 с причинами, короткая запись в свидетельстве стадии о том, какие числа сошлись с боевой.

Stop: остановись и доложи владельцу, когда стенд готов, — платный прогон запускается отдельной сессией и отдельным разрешением. Остановись, если числа на стенде разойдутся с боевой: это значит, что расстановка воспроизвелась не полностью, и мерить на ней нельзя.
