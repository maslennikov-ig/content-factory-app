Target: /home/me/code/content-factory-next, ветка codex/content-bugs, worktree /home/me/code/.worktrees/cf-cj4
Audience: рабочая сессия-исполнитель
Runtime: Claude Code CLI

Goal: довести шестую волну эпика `content-factory-next-pl1`. Четыре задачи, условия и приёмка в них самих — читай через `bd show`, не пересказывай себе заново: `pl1.30` (экран показывает две доли ошибок), `pl1.31` (шестнадцать тем и прогон), `pl1.32` (продукт запоминает правки автора), `pl1.34` (повторный разбор после выката). `pl1.33` ждёт `pl1.31` и в эту волну не входит.

Порядок работ. `pl1.30` и `pl1.32` бесплатны и идут первыми, в любом порядке. `pl1.31` делится надвое: подготовка бесплатна, прогон платный и запускается ОТДЕЛЬНЫМ разрешением владельца. `pl1.34` — после выката, не раньше.

Context: прочитай `.codex/handoff.md`, `.codex/stages/content-factory-next-pl1/evidence/README.md` и коммит `93f7737a` целиком — в его сообщении лежит всё, что было измерено 27.08.2026, и оно не повторяется здесь.

Коротко о том, что изменилось и почему это меняет чтение старых чисел:

- Подставными были три файла документации этого репозитория. Подозреваемый пишет посты в Telegram, а в шеренге стояли руководства по развёртыванию. Шеренга теперь собирается на разборе из настоящих коротких текстов других авторов системы — `lineup.ts`, `voice-calibration.ts`, и та же арифметика у стенда через `scripts/evidence/voice-eval/foreign-corpora.cjs`.
- Порогом была константа `2/3`. Снята. Два порога на автора, между ними полоса «не могу сказать» — `voice-calibration.ts`.
- Профиль вырос с 400 окон до 1000, `NGRAM_MEASURE_VERSION` двинут.
- **Выводы четвёртой волны ничем не обеспечены.** Они сняты на корпусе владельца, а на перестроенной мерке там ни один вариант не отрывается от нуля: рабочая точка 13,3%, вся генерация между 0,4% и 9,6%, все интервалы накрывают ноль. Прогоны `owner-2026-08-25-f`, `owner-2026-08-25-g` и `pooled-owner-2026-08-26-b+owner-2026-08-26-c` пересчитаны 27.08.2026 и показывают это. Сигнал несут `avetov` и `britva`. Не опровергнуто — не обеспечено; разница существенная, и в свидетельство её надо писать именно так.

Одно измерение, которое стоит держать в голове при любом изменении мерки: **материал на подставного решает больше, чем число подставных.** Шесть, двенадцать и двадцать кусков одних и тех же двух корпусов дали 0,826, 0,823 и 0,819 среднего AUC — монотонно вниз. А семь тысяч знаков на подставного против двадцати девяти дали худший запас 26,8 против 59,0.

Success criteria: `pl1.30`, `pl1.32` закрыты по своей приёмке. `pl1.31` подготовлен целиком и остановлен перед платным прогоном. `pl1.34` описан как шаг выката и не выполнен раньше выката. Полный Jest зелёный, `npx tsc --noEmit -p libraries/nestjs-libraries/tsconfig.lib.json` не даёт новых ошибок сверх пяти, унаследованных из HEAD.

Constraints:

- Платные вызовы — только с явного разрешения владельца, каждый раз заново. Прогон `pl1.31` стоит около 0,22 доллара на flex; на счёте OpenRouter было 2,81, и там же зафиксирован расход около 0,045 в минуту от трафика, который не наш. Проверить кабинет до любого платного шага.
- Боевую не трогать. Схему применять точечным `psql` в одной транзакции: `prisma db push` снесёт 29 таблиц `mastra_*`, число таблиц сверять до и после.
- Кэш корпуса и `generations.json` остаются вне репозитория: в них чужие тексты.
- Шеренга и порог читают тексты других аватаров системы. Через границу арендатора ходят только числа. Ни одно чужое предложение не должно оказаться в базе, в промпте, на экране, в логе или в свидетельстве — на это есть тест `tests/brand-voice.routes.test.cjs`, «через границу не переходит ни одного чужого предложения», и он должен остаться зелёным после любой правки этого пути.

Ловушки:

- `/home/me/.local/bin/node` затеняет nvm — ставь `PATH` явно.
- Jest не проверяет типы. Гони `tsc --noEmit` до сборки образа.
- Полный Jest оставляет фронтенд отвечающим 500, пока не удалить `apps/frontend/.next`.
- Temporal на 7234: если бэкенд висит на старте, `docker start cf-dev-temporal`.
- Prisma отвергает `null` внутри `in` — про nullable-колонку спрашивают через `OR`. И `{ not: 'x' }` над nullable-колонкой ведёт себя по-разному в разных версиях: пиши `AND: [{ col: { not: null } }, { col: { not: 'x' } }]`.
- Beads откатывает закрытия при работающих агентах. Закрывай одной партией в конце, сразу `bd dolt push`, потом сверяй поимённо через `bd show` — вывод самой `bd close` рапортует успех и до отката.
- Красная проверка обязана краснеть. Первая версия набора `tests/brand-voice.calibration.test.cjs` пережила подмену выбранного порога на константу `2/3` — данные не отличали одно от другого. Подменяй ровно тот кусок, который сторожишь, и смотри, какие проверки падают.

Output: правки с тестами, коммиты в `codex/content-bugs`, запись в `.codex/stages/content-factory-next-pl1/evidence/README.md` о том, что измерено и что осталось неизвестным, обновлённый `.codex/handoff.md` (потолок 200 строк, старое ужимать, а не дописывать сверху).

Stop:

- Остановись перед платным прогоном `pl1.31` и доложи владельцу: сколько генераций, сколько вызовов, сколько это стоит и что именно прогон решает.
- Остановись, если после шестнадцати тем интервалы всё ещё накрывают ноль на всех трёх корпусах. Это будет означать, что дело не в числе тем, и следующий шаг — не третий прогон, а разбор того, почему разброс между темами так велик.
- Остановись перед выкатом `pl1.34`, если межарендаторная обработка ещё не названа в документах о данных.
