Target: /home/me/code/.worktrees/cf-draft-pick, ветка `codex/pl1-draft-pick`
Audience: рабочая сессия-исполнитель
Runtime: Claude Code CLI

Goal: `pl1.33` — отбор черновиков по голосу. Продукт пишет черновик, оценивает его мерой похожести на автора и, если не прошёл, пишет второй; при необходимости третий. Адаптивно, а не всегда три. Условия и приёмка в самой задаче, читай через `bd show content-factory-next-pl1.33`, не пересказывай себе заново.

Context: прочитай `.codex/handoff.md`, `.codex/stages/content-factory-next-pl1/evidence/README.md` целиком и коммит `93f7737a` целиком — в его сообщении вся арифметика 27.08.2026. Код: `agent.graph.service.ts`, `voice-directives.ts` и — только на чтение — `voiceprint.ts`, `voice-calibration.ts`.

Это трек B. Параллельно идёт трек A (ревизия эпика и `pl1.5`, перекалибровка мерки) в дереве `/home/me/code/.worktrees/cf-measure`. Ты в его файлы не пишешь, он в твои — тоже. Твоя зона: `libraries/nestjs-libraries/src/agent/` и вариант стенда в `scripts/evidence/voice-eval/variants.cjs`. Его зона: `content-intelligence/brand-voice/` и остальной стенд. Мерку читаешь, но не правишь: если кажется, что её надо менять, это находка для трека A — запиши её и скажи корневому сеансу.

Почему задача построена измеримой, а не «улучшающей». Оба исследования 27.08.2026 называют критик-и-правку единственным кандидатом, совместимым с обычным chat API, и оба честно говорят, что на метрике авторского сходства он не проверен. PersonalBench показывает, что потолок промптинга не сдвинулся: четыре способа персонализации дают 0,484–0,508 при человеческом поле 0,626. Узел не считается удавшимся, пока разница не измерена той же меркой, что судит всё остальное.

Что знать про мерку, которой пользуешься. Замер 28.08.2026 на боевой: у всех трёх голосов нижняя граница **ноль**, верхние — 0,25, 0,4 и 0,75. То есть «не похоже» выносится крайне редко, а в полосу «не могу сказать» попадает от четверти до трети собственных постов автора. Следствие: отбор по вердикту почти ничего не отсеет. Строй отбор на **числе голосов**, а не на вердикте, и бери лучшего из кандидатов — это и записано в задаче пунктом 4. Трек A прямо сейчас переснимает порог против генерации, поэтому числа под тобой изменятся: читай калиброванную точку из измерения, а не из константы, тогда узел переживёт смену мерки без правок.

Success criteria: узел стоит после `generate-content`, оценивает черновик голосованием против отпечатка аватара и калиброванной точки автора, берёт лучшего кандидата по голосам, имеет объяснённый потолок попыток и прозрачен без активного голоса. Стенд умеет прогнать вариант с отбором и без. Полный Jest зелёный. `tsc --noEmit` не даёт новых ошибок сверх двух унаследованных (`text-spots.ts:315`, `voice.service.ts:1113`). Каждая новая красная проверка проверена подменой того, что она стережёт. Замер разницы — отдельным шагом, после разрешения на платный прогон.

Constraints:
- Платные вызовы — только с явного разрешения владельца, каждый раз заново. Код и проверки бесплатны; замер платный. До любого платного шага проверить остаток на счёте OpenRouter, ключ в чат не выводить. `--tier flex` обязателен: на его отсутствии уже переплатили $0,54 вместо $0,22.
- Каждый лишний черновик — деньги пользователя. Потолок попыток и условие выхода должны быть видны в коде и объяснены, а не выведены из общих соображений.
- Боевую не трогать. `prisma db push` не применять никогда.
- Кэш корпуса и `generations.json` остаются вне репозитория — в них чужие тексты, `.gitignore:99` это держит.
- Ни одно чужое предложение не должно попасть в базу, промпт, экран, журнал или свидетельство. В отчётах только числа. `tests/brand-voice.routes.test.cjs` обязан остаться зелёным.
- Контракт Temporal не менять: если узел меняет форму workflow или activity, заводится версионный преемник, старый остаётся.
- Beads не трогаешь вовсе: закрытия ведёт трек A, две сессии одновременно теряют закрытия. Что нужно записать в задачу — скажи корневому сеансу.

Ловушки: `export PATH="$HOME/.nvm/versions/node/v22.23.2/bin:$PATH"` обязателен, иначе берётся Node 24 и `public-funnel-runtime-proof` падает на проверке версии — это не регрессия. Полный Jest оставляет фронтенд отвечающим 500, пока не удалить `apps/frontend/.next`. Правки в `libraries/` требуют удалить `apps/backend/dist` и `tsconfig.tsbuildinfo`. `tsc --noEmit` — отдельный шаг, Jest типы не проверяет.

Output: правки с проверками, коммиты в `codex/pl1-draft-pick`, запись в `evidence/README.md` в своём разделе — трек A пишет в тот же файл, поэтому добавляй в конец и не переписывай чужое. `.codex/handoff.md` не трогай: его ведёт трек A, иначе слияние станет конфликтом на каждой строке. Ветку не сливать: слияние в `codex/content-bugs` делает корневой сеанс, когда оба трека закончат.

Stop: остановись и спроси, если нужен платный прогон; если выяснилось, что отбор невозможно измерить нынешней меркой — это находка для трека A и решение владельца, а не повод изобретать свою мерку; если узел требует изменить контракт Temporal.
