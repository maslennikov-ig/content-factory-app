# План `content-factory-next-71m.5`

1. Зафиксировать текущий путь запуска от `main.ts` через Nest-модули, Prisma,
   IntegrationManager, social providers, Mastra/MCP и CopilotKit.
2. Измерить импорты в отдельных процессах Node 22.23.2 без сети и БД; не
   складывать пересекающиеся значения как независимые части RSS.
3. Выбрать самый узкий boot-reachable импорт с измеримой ценой и перенести его
   за первый реальный вызов, сохранив provider contracts.
4. Доказать RED/GREEN для ленивой загрузки и прежних OAuth/provider shapes.
5. Выполнить один root-owned release closeout, обновить Beads и закоммитить
   ровно один раз с указанием модели Сол.

Не входит: подключение к локальной или боевой БД ради измерения Prisma engine,
запуск production-контейнера, переработка DI-графа целиком, lazy Mastra/MCP и
изменение списка социальных платформ.
