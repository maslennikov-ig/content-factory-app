# Волна второго захода 08.09

Контракт: docs/prompts/astra-second-walk-wave-2026-09-08.md. Beads — источник статусов. Приёмка и выпуск принадлежат root. Перед выпуском обязателен показ стенда владельцу. Простой S5 — Luna Max; S1 — Sol High; S2/S6 — Astra Medium. Изолированные рабочие копии нужны для параллельной реализации. Платные проверки в тестах запрещены.

## Условия выпуска

Использован technical-premortem. GO WITH CONDITIONS: публикация при EVIDENCE_REQUIRED сохраняет строгую проверку; потоки выдают первую строку без буферизации; review не списывает вызов при чтении страницы и не меняет текст до «Принять»; tenant scope проверяется в двери и записи. Стыки файлов интегрируются по заданному порядку. Непустой schema diff блокирует выпуск.

Откат: switch-host-image.sh 5f657ccf294e, затем healthy и CONTENT_FACTORY_RELEASE; схема не меняется, ожидаемое окно — таймаут health скрипта 180 с плюс запуск. Частично выполненный review не сохраняет проверку.

Исполнительская ошибка: публичная история и незакоммиченные приватные файлы не должны попасть в образ; сборка только из public tree с Source-Commit и suite receipt, remote fetch перед каждым push.

Документация: docs-resolve compression 1.8.1 вернул fallback-needed (L1 404), S2 проверяет first-party и installed code. Graphify owner graph от 94fdcb33 устарел, ContentPiece отсутствует; текущие файлы подтверждаются напрямую.

Дизайн: макеты Claude неизменны, выбран A. Lazyweb — таблицы Copy.ai/Canva как вспомогательные примеры состояния внутри ячеек и постоянной навигации; решения владельца не меняются. Частная подборка: https://www.lazyweb.com/agentic-search/bc71a4e0-ca6c-430b-bd40-4f0549fa2db7.

Уборка по разрешению владельца 08.09: удалены два остановленных старых cf-dev контейнера после замены; cf-dev-postgres-data сохранён из-за существующих данных. Новый стенд использует cf-cj4_postgres-volume; production копия восстановлена локально, временный dump удалён. Удалён также старый stopped Temporal без томов. Локальная уборка образов завершена: 62 тега / 31 старая версия; сохранены 5f657ccf294e и 47cd8475c442. Контейнеры других проектов не затронуты.

S8: dc1a8899 + 38d287f0 + 0bb2ed47; 4/4 Jest (входит в root suite). Копия production 5f657ccf294e восстановлена локально без остановки сервиса; dump удалён. 2 мысли, 92.5% слов в сути, подходящих адаптаций нет — ограничение данных, не доказательство переноса в адаптацию. Отчёт evidence/liveness-2026-09.md.

## Корневая интеграция

S5 → S1 → S2 → S8 → S3 → S4 → S7 → S6 приняты по порядку. Конфликты: neutral intake сохраняет foreignShingles; PieceScreen сохраняет S3 варианты/разницу и S4 один объект адаптации; S7 принимает только совпадающий draft.adaptationId; S6 не восстанавливает снятый archive view. Все рабочие копии потоков очищены после чистого статуса и проверки доставки. Beads пока открыты, общая партия после приёмки.

Общий release_commands сохраняет build, brand, docs, process и diff; добавляет tsc трёх приложений, а pnpm test вызывается один раз внутри record-suite-receipt.sh. Это тот же полный набор с квитанцией на точный HEAD.

docs-reviewed: updated - FAQ, tariff, component inventory, project index and current handoff reflect the integrated paths. Runbook records current release authority and verified release 2fe4032ea3db.
graph-reviewed: no-change-needed - owner graph used read-only for orientation; stale entries were not treated as current, exact changed paths confirmed in the accepted tree. Optional graph refresh is not a release requirement.

Стенд: dev watch после массовой интеграции исчерпал стандартный Node heap; перезапущен с 8 GiB. Повреждённый временный Next dev cache перемещён в /tmp для безопасного пересоздания. Production build/cache и данные не менялись.

Просмотр владельца и текущие поправки зафиксированы в evidence/owner-stand-2026-09-08.md. UI-правки, отключение автоматической проверки чисел и отдельная явная проверка поиском выполняются перед повторным показом; выпуск ещё не выполнен.

Все дополнительные правки владельца интегрированы: кнопки/подписи a09b5b4e, таблица/ширина/общий стиль 0e5f6f9b+7e837750+3f918453, без автопроверки0d48cbcb, явный web review71dd6ab6. Рабочие копии корректирующих потоков очищены после проверки доставки. Повторная browser-проверка1440/390 успешна без платных запросов; общий прогон ещё впереди. Для чистой release-приёмки используется отдельная root-owned рабочая копия, поскольку в основном каталоге остаются чужие незакоммиченные docs/Lazyweb изменения.

Первый общий прогон на0a2a5cb5: tsc трёх приложений и build прошли; Jest407 наборов,398 прошли/9 не прошли (19 проверок), поэтому Node/Python ещё не запускались и квитанция выпуска не выдана. Найдены старые ожидания API/ролей/навигации и фикстуры языка, пропущенные переводы роли review/кнопки повтора, сырой select канала. Root исправил общий Select, общий ключ названия «Заготовки», существующий try_again, переводы всех языков и точные ожидания API/матрицу ролей; семь затронутых наборов155 тестов прошли. Три набора sidebar/метаданных исправляет отдельный ограниченный поток. Полный повтор обязателен на финальном коммите.

Коррекция фикстур933b976e принята какdec03bca: три набора65 тестов прошли, рабочая копия очищена. Все девять первоначально упавших наборов исправлены и проверены узко; далее полный повтор, без переименования старой квитанции или ослабления проверок.

Дополнение .26: утверждённые макеты «Каналы» из a111b5f0 объединены как f748abb5 без изменений. Три изолированных потока: API и статистика (Sol High), общие поля профиля и встроенная правка (Sol High), страницы списка/канала и существующие действия (Astra Medium). Первые 25 критериев и release сохранены в неизменном снимке, точное соответствие и добавленный критерий записаны в scope-preservation-ledger.json. Второй общий прогон остановлен при добавлении объёма; финальный полный прогон требуется после объединения. До выпуска нужен явный ответ владельца «да» по разделу на localhost:4200.

Раздел .26 объединён: API b7c9c282, общий профиль a039610e, интерфейс af7d3063. Чистые рабочие копии удалены после сверки доставки. Реальные HTTP list/posts200, счётчики совпадают, limit11 отклонён400; browser1440/390 показывает таблицу с сохранением выбора, четыре панели/мобильные вкладки без переполнения и JS-ошибок. Три канала проверены компонентными фикстурами; на локальной копии один. Явное «да» владельца и финальная приёмка ещё впереди. Ограничения: общий список заготовок без фильтра канала; группа назначается меню после подключения; публичная ссылка и имя бота скрыты, когда API их не предоставляет.

Финальный прогон 1347afad: три tsc и build прошли; Jest406/410 наборов,5265/5269 тестов. Четыре причины исправлены без ослабления условий: браузерная фикстура маршрута явно переключает i18next; реальный nginx-тест принадлежности лимита использует медленное восстановление только в тестовом конфиге (боевой5r/s отдельно проверяется прежним стражем); два новых отступа профиля10px заменены на12px; страж навигации ожидает утверждённые ChannelsScreen/ChannelScreen вместо отклонённой рейки. Точечная повторная проверка76/76 прошла. Node/Python ещё не запускались, общей квитанции нет. Полный повтор обязателен.

Дополнение .27–.30: утверждённый календарный холст00dc2063 объединён как72374ff1. Один общий выпуск, прежние критерии сохранены; добавленные критерии в ledger. Потоки API готовых адаптаций/связи поста (Sol High), календарь/выбор/предпросмотр (Astra Medium), меню/онбординг/помощь «Контент» (Luna Max). Для плюса выбран разрешённый вариант «дата не выбрана»; для клетки передаётся её точное время. Повтор приёмки3864012d остановлен при добавлении этой работы; полного успешного прогона пока нет.

Меню .30 принято: f6c0272c → 55e25660. Контент / Content в меню, онбординге и помощи; имя внутренней таблицы сохранено. Два набора51/51, полный прогон после календаря.

Интерфейс .27–.29 принят: 88c88abb → 3770097a. Frontend tsc и96 уникальных целевых тестов прошли. Рейка удалена, канал в URL и обоих запросах, выбор открывает прежний редактор с датой/каналом, происхождение из защищённой выдачи. API ещё интегрируется; весь критерий пока не принят.

API .27–.29 принят: eaa31051 → f728f92f. 104 Jest +12 Node, backend tsc прошли. Фильтр до пагинации, tenant/customer/channel условия объединены; защищённое происхождение есть в календаре, списке и группе. Далее реальный HTTP/browser и явное согласие владельца.

Root реальный API после полной пересборки локального backend: ready200, одна DRAFT адаптация, limit51→400, calendar/list/group piece сохранён, неверный канал list total0, AiUsageRecord delta0. Windows Chrome /launches200, рейки0, picker1 строка, время клетки сохранено, ошибок0. Повтор CDP для открытия самого редактора дважды timeout на подключении; этот переход подтверждён целевыми UI тестами, но не дополнительным browser proof. Новая вкладка календаря открыта владельцу через существующий Windows Chrome. Явное согласие владельца запрошено и пока не получено.

08.09 владелец ответил «да» на вопрос о новом календаре и Каналах на localhost:4200. Условие явного просмотра до выпуска выполнено; далее финальный полный прогон и выпуск всей волны.

Полный прогон9f024f6d: три tsc и build прошли, Jest404/411 наборов и5254/5266 тестов. Семь наборов остановили квитанцию; Node/Python не запускались. Root поправил живой источник языка новых подписей календаря/выбора/предпросмотра и заголовок вкладки Content, сохранив имя внутренней таблицы. Тестовые ожидания перенесены на фактические новые поверхности: Channels CalendarContext, реальные значки/аватары, соседство фильтров, имя раздела в онбординге. Пять наборов прошли в первом узком запуске, два исправленных повторно66/66; итого114 уникальных проверок. Отдельный PDF IPC сбой разбирается изолированным Sol High; полный повтор после его доставки. Согласие владельца сохраняется.

PDF release-tail91402069 → c0959e75 принят: PDF/DOCX process.send ждал только постановку в очередь, process.exit терял ответ. Теперь callback доставки ожидается перед естественным выходом. Границы timeout/SIGKILL/памяти и классификации прежние. 3 набора21/21, targeted tsc0,120 реальных PDF разборов без неверной классификации. Root просмотрел diff; внешнее поведение подтверждено Node22 docs после docs-resolve. Все семь упавших наборов исправлены; полный повтор обязателен на новом HEAD.

## Выпуск принят 08.09.2026

Полная приёмка `7b07bec6a989` прошла:9 настроенных команд и точная process-проверка; Jest411/5270, Node124/0 с4 прежними пропусками по окружению, Python46. Предыдущая попытка211fb696 остановилась только на154 строках project-index; индекс сокращён до149, ссылки сохранены, stale upstream исправлен. Полный повтор выполнен штатно, квитанция не редактировалась.

Выпущен публичный `2fe4032ea3db`, source `7b07bec6a98924b9975493fee7fb9bb31091b032`, digest `sha256:e257a445e02d7f9a5dc75ca8f1ea36b55e2f2e1346089e473e2f3aa64127d686`. Стенд явно одобрен владельцем. Миграций нет: diff0; Mastra0→0 в продуктовой базе,29→29 в отдельной, структура совпадает. Healthy,0 перезапусков, маркер версии совпал,0 ошибок запуска; API/login/source200, архив совпал по SHA-256. Штатное удержание оставило новый образ и откат `5f657ccf294e`,23ГБ свободно.

30 задач закрыты одной партией после завершения всех исполнителей;30 поимённых reread подтвердили closed,30 штатных GitHub sync triggers отправлены. Эпик закрыт и перечитан; отдельный штатный триггер синхронизации отправлен. Запись выпуска сохранена через bd remember. Доказательство: evidence/release-2026-09-08.json. Документальный коммит после выпуска не выдаётся за новый проверенный image source. Claude готовит третий живой проход.

## Cleanup / closeout 08.09 после выпуска

Владелец запросил полное завершение и подтвердил включение шести незакоммиченных правок Lazyweb-документации в main. История волны fast-forward объединена в main, правки включены как9eb05aa1; origin/main обновлён. Код apps/libraries/scripts/deploy, Dockerfile, package.json, lockfile и var/docker совпадает с проверенным7b07bec6: повторный выпуск образа не нужен. Исходная квитанция неизменна.

31 локальная ветка удалена после проверки ancestry;11 слитых удалённых веток удалены. Две старые несмерженные ветки orchestration (8c6ca480,a1d6b1ea) сохранены в проверенный bundle внутри.git и удалены только после явного ответа владельца. Технический ref Beads сохранён. Открытых PR нет. Оба временных worktree и временный публичный клон удалены; основной public clone чист на2fe4032ea3db, архив исходников и suite receipt перенесены и проверены. Дубликат шести изменений public clone сохранён в private main и.patch перед восстановлением публичного дерева.

Локальный стенд: подтверждённая группа процессов8410 остановлена;6 cf-dev контейнеров удалены поимённо, тома не удалялись. Production проверен:2fe4032ea3db healthy,0 перезапусков; только новый и rollback5f657ccf294e,23ГБ свободно. Эпик и30 задач повторно перечитаны:closed. Handoff больше не предлагает повторный запуск закрытой волны.

docs-reviewed: updated - main integration, archive decision, stopped local stand and next Claude walkthrough recorded.
graph-reviewed: no-change-needed - cleanup changed refs/docs/runtime leftovers only; application source is identical to the accepted release.
